# История изменений и проверок

Хронология изменений и результаты проверок. Поведение программы описано в [README.md](README.md); здесь — что и когда менялось, какие проверки прошли и что осталось непроверенным. Логи и снимки лежат в `results/` (не в Git). Исторические логи описывают прошлые прогоны, а не свежую валидацию текущей сборки.

## 2026-09-25 — релиз v0.2.13 опубликован

- Публичный коммит `5754264aa1d15b293a4feb3ae6ea3287c635534b`; локальный runner [run 36173435058](https://github.com/Arkanoidvfx/MicNoize/actions/runs/36173435058) завершился успешно. Все 10 ассетов совпали по SHA-256; runner удалён, TAG-хост продолжил работу. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.13.
- Полный пакет: 20 626 354 байта, SHA-256 `5c0b4738b02570903dcfd4dfc68b31b84104d6168884ee4a104746b09248aae2`; `Setup.exe`: 25 087 922 байта, SHA-256 `201703a9ca4df2130ca5ba3d12c2f3c50c4311ac13ef2f6a8bec19d7b1ff9007`.
- Прошли `verify.ps1`, упаковка и сверка ассетов. Реальное шипение микрофона и мерцание в живом окне, а также отключение питания во время обновления не проверены.

## 2026-09-25 — патч 0.2.13: индикатор «До / После»

- На 0.2.12 шкала начиналась с −60 дБ, поэтому в тишине индикаторы стояли, хотя микрофон без шумодава шипит; «До» и «После» выглядели одинаково.
- Шкала теперь от −90 дБ. Новый виджет `tacho::level_meter`: «До» — 60 столбиков неровной высоты и яркости, мерцающих при каждом обновлении уровня, с «помехами» над пустой частью; «После» — 40 скошенных сегментов в стиле ползунков: градиент от тёмно- к ярко-зелёному, светящийся крайний сегмент с ореолом, метка пика держится 0,9 с и плавно опускается; выше 93 % шкалы — оранжевый. Первый вариант (гладкая полоса) отклонён как скучный; из пяти вариантов на холсте (сегменты, осциллограф, история, жила, студийная шкала) выбраны сегменты.
- Прошло: `cargo test --release --locked` — 56 passed, 2 ignored; clippy `-D warnings` чисто; снимок `results/design-0.2.13/design-main.png`.
- Не проверено: как выглядит реальное шипение конкретного микрофона на новой шкале, мерцание в живом окне.

## 2026-09-25 — релиз v0.2.12 опубликован

- Публичный коммит `46c46f94b2128577030a195aa9b086dd3c9a1114`; локальный runner [run 36153534327](https://github.com/Arkanoidvfx/MicNoize/actions/runs/36153534327) завершился успешно. Все 10 ассетов совпали по SHA-256; runner удалён, TAG-хост продолжил работу. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.12.
- Полный пакет: 20 624 294 байта, SHA-256 `daf04bec5c48946ec100c181fb6c6f0ecbc835aad8f58955b998a5a2c1ea49b7`; `Setup.exe`: 25 085 862 байта, SHA-256 `2be67185dd14666758b67cf960167905b227987579e5198fcfece4e804c7ec69`.
- Прошли `verify.ps1`, упаковка и сверка ассетов. Живое перетаскивание через 100 %, поведение рамки после мыши в установленной сборке и отключение питания во время обновления не проверены.

## 2026-09-25 — патч 0.2.12: рамка ползунков и перетаскивание через 100 %

- На установленной 0.2.11: вокруг ползунка после работы мышью оставалась тонкая оранжевая рамка фокуса; перетаскивание «Силы» обрывалось на переходе в красную зону — слой лент добавлялся в `stack` только выше 100 %, ползунок смещался в дереве виджетов и терял состояние «зажато».
- Рамка ползунков теперь показывается только после клавиатуры (`focus_visible`: истина при нажатии клавиши, ложь при клике мышью); сам фокус для стрелок остаётся. Слой лент всегда в дереве и рисуется только выше 100 % (при скрытии анимация появления сбрасывается).
- Прошло: `cargo test --release --locked` — 56 passed, 2 ignored; clippy `-D warnings` чисто; снимки `results/design-0.2.12` (ленты при 135 %, нет при 99 %).
- Не проверено: живое перетаскивание через 100 % и отсутствие рамки после мыши в установленной сборке.

## 2026-09-25 — сверка локальной документации после 0.2.10

- README и рабочий skill приведены к фактическому пути `D:\Projects\Audio\MicNoiseReducer` и локальному одноразовому runner как основному способу выпуска; 0.2.11 явно отмечена как невыпущенная работа. В документе TAG отражена проведённая после первоначальной записи проверка перезагрузки.
- Проверено: GitHub показывает 0.2.10 последним релизом, установленный `sq.version` на этом ПК — 0.2.10, обновлённые пути и команды указывают на существующие файлы проекта. Сборка приложения для правок документации не запускалась; способ перехода установленной программы с 0.2.9 и откат отдельно не проверялись.

## 2026-09-25 — релиз v0.2.11 опубликован

- Публичный коммит `af08f682ce722fe6463fd1a8be9f1eb51cd6fa15`; локальный runner [run 36150113654](https://github.com/Arkanoidvfx/MicNoize/actions/runs/36150113654) завершился успешно. Все 10 ассетов совпали по SHA-256; одноразовый runner удалён, TAG-хост не останавливался. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.11.
- Полный пакет: 20 624 944 байта, SHA-256 `eb40d32641f6095d36ccdcfbeeaae6a3f9bb7bc74bd666a886d9a638797da9ce`; `Setup.exe`: 25 086 512 байт, SHA-256 `17ed470aff98468c79818c8ae41f7243dfa6144c7b6949dc18f401c77c3a4df4`.
- Прошли `verify.ps1`, упаковка и сверка ассетов. Живое окно на масштабах 125–200 %, субъективное восприятие анимаций и отключение питания во время обновления не проверены.

## 2026-09-25 — патч 0.2.11: доработка интерфейса

- На установленной 0.2.10 найдено: нажатая клавиша в хоткеях почти не отличалась и переключалась мгновенно; у «Силы» выше 100 % по окну расползались красные полосы — бегущая надпись ленты частично выходила за пределы ленты, tiny-skia рисовал её без маски и никогда не стирал; ленты шли ровно по горизонтали и прятались за шкалой; буквы слова реверса стояли в фиксированных ячейках (Ж слипалась с соседями); на «Смене голоса» были большие отступы у шкалы, а дополнительные параметры требовали прокрутки.
- Кейкап — свой виджет (`tacho::keycap`): при нажатии проседает до 4,5 px с уменьшением до 90 % и оседает на 3 px (280 мс), становится оранжевым с ореолом; отпускание — 240 мс с небольшим подскоком. Ленты «экспериментально» рисуются геометрией с поворотом −7° и +11° (надпись — контурами глифов, обрезается карточкой), перекрещиваются над шкалой. Буквы реверса расставлены по собственной ширине с зазором 1,5 px и меняются местами с учётом ширин. «Смена голоса»: тон и «Тонкая настройка» — две карточки рядом, шкалы по 16 сегментов, страница помещается без прокрутки.
- Прошло: `cargo test --release --locked` — 56 passed, 2 ignored; clippy `-D warnings` чисто; `verify.ps1`, release-сборка и упаковка; снимки `results/design-0.2.10` (в том числе `effects-ctrl-held`, `rvc-advanced`).
- Не проверено: живое окно (анимация нажатия, ленты в движении, реверс с широкими буквами), масштаб 125–200 %.

## 2026-09-25 — релиз v0.2.10 опубликован

- Публичный коммит `4147cbe85f9dcf4ec6e86b6c6b064e8dab500a0a`; локальный GitHub runner [run 36144041793](https://github.com/Arkanoidvfx/MicNoize/actions/runs/36144041793) завершился успешно за 2 мин 58 с от запуска до завершения. Все 10 ассетов совпали по SHA-256 с локально проверенной сборкой; runner удалён, работающий TAG-хост не останавливался.
- Полный пакет: 20 613 796 байт, SHA-256 `9c0e9847f42a0f4d7fe5096e3a1ef4621cc4a0a28f26f1272e7ec4942df23080`; `Setup.exe`: 25 075 364 байта, SHA-256 `15f8305702881a527fdb7517618dc17f6e0a6ebeb03e2307cb367eceb75cafa1`. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.10.
- Проверены автоматические тесты и упаковка; на момент публикации установленная 0.2.9 ещё не обновлялась до 0.2.10. Позднее на этом ПК появилась 0.2.10, но способ перехода и откат отдельно не воспроизводились; живой интерфейс на 125–200 % и фактическое отключение питания при обновлении не проверены.

## 2026-09-25 — патч 0.2.10: исправления отрисовки после 0.2.9

- На установленной 0.2.9 найдено: название устройства в списках при первом показе накладывалось на старое (tiny-skia перерисовывает у центрированного по вертикали текста `pick_list`/`text_input` только нижнюю половину, наведение исправляло); кейкапы хоткеев становились чёрными — тень tiny-skia с нулевым размытием даёт деление на ноль и рисуется поверх частичных перерисовок; анимации нажатия клавиш не было вовсе; реверс шёл ~30 кадров/с; слово реверса принималось только Enter.
- Списки и поля ввода обёрнуты невидимым фоном, меняющимся вместе с подписью, — при смене текста перерисовывается всё поле. Тени убраны: у кейкапов нарисованный «бортик», у панели наушников тёмное кольцо. Пока окно в фокусе, UI раз в 50 мс читает `GetAsyncKeyState` только для назначенных клавиш и модификаторов; нажатая клавиша опускается и становится оранжевой. Реверс анимируется каждый кадр; клик мимо поля слова завершает ввод. Свечение ползунков ослаблено (радиус 11/7 px, яркость 0,6).
- Прошло: локальный `verify.ps1` за 69,9 с — 3/3 CTest, 56 Rust tests passed, 2 ignored, release clippy чисто; `scripts/check-release-local.ps1` отклонил повреждённый ассет и неверный пакет отката; снимки в `results/design-0.2.10`.
- Не проверено: живое окно (подписи списков после обновления устройств, кейкапы при нажатии, завершение ввода кликом), масштаб 125–200 %.
- Версия обновлена в `release/version.txt`, `ui/Cargo.toml` и `ui/Cargo.lock`; 0.2.10 прикладывает `Repair-0.2.8-update.ps1` для старого обновлятора 0.2.8. Скрипт по-прежнему работает только при установленной 0.2.8 и сверяет SHA-256 её официального пакета.
- Первый локальный прогон собрал пакет и загрузил 10 файлов в непубличный черновик, но проверка черновика через `gh api releases/tags` получила 404. Проверка переведена на `gh release view`, добавлен тест возобновления совпадающего черновика без повторной загрузки. Черновик удалён перед повторным выпуском.

## 2026-09-25 — одноразовый локальный runner для следующих патчей

- Вместо ручного ввода секрета и 12-минутной GitHub-hosted сборки добавлены `release-local.yml` и `scripts/run-local-release.ps1`: локальный одноразовый runner берёт существующий секрет из GitHub Actions, проверяет/собирает в прогретом рабочем проекте, сверяет загруженные файлы и публикует релиз. Runner регистрируется с одной собственной меткой только на время задания и удаляется после него; `-Smoke` проверяет соединение без публикации.
- 0.2.9 на GitHub-hosted runner заняла 12 мин 50 с, хотя Rust-кэш (~615 МБ) восстановился: основной этап проверки/упаковки занял почти 10 минут. Локальный `verify.ps1` прошёл за 4,9 с. Пробные задания локального runner прошли с секретом и GitHub-токеном без ручного ввода (`36140164121`); полная проверка, сборка и упаковка без публикации заняли 2 мин 11 с (`36140539333`), 9 файлов пакета прошли проверку SHA-256. После заданий runner удалён. Следующий реальный релиз 0.2.10 с загрузкой и публикацией занял 2 мин 58 с (подробности выше).

## 2026-09-25 — релиз v0.2.9 опубликован

- В релиз входят сглаженная отрисовка элементов нового UI, исправление остаточных фрагментов текста, волна последней записи и сохранение текущего полного пакета до загрузки следующего обновления. Ниже описаны причины и детали обеих правок.
- Для перехода с уже выпущенной 0.2.8 приложен `Repair-0.2.8-to-0.2.9.ps1`: после ошибки старого обновлятора он восстанавливает SHA-256-проверенный официальный пакет 0.2.8 для отката. Установленную программу и TAG-хост скрипт не заменяет.
- Версии в `release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock` и текст `release/notes.md` переведены на 0.2.9. Полный `verify.ps1`: 3/3 CTest, 56 Rust tests passed, 2 ignored; release clippy чисто. `scripts/check-release-local.ps1` отклоняет изменённый ассет и неверный пакет отката. Снимки интерфейса — в `results/design-0.2.9`.
- Ещё не проверены: реальный переход установленной 0.2.8 → 0.2.9, исправленный UI в живом окне на масштабах 125–200 %, нагрузка анимаций на CPU, реальное отключение питания во время обновления.
- Публичный коммит `0a044c9ba892ff6adc195bec86cc96116d864ade`; GitHub Actions run `36136481339` завершился успешно за 12 мин 50 с. Предыдущий Rust-кэш (~615 МБ) восстановился, но этап проверки/упаковки занял почти 10 минут после изменения UI-зависимости. Полный пакет: 20 640 234 байта, SHA-256 `e1aa44b73059de06e4538f034df80ed90f393f24969c21424df5c330bc3c180b`; `Setup.exe`: 25 101 802 байта, SHA-256 `ebe63b7c13bb9c8533f2b7b2487dc3071b5b58bf5ac67b25e2610681fdf11464`. Скачанный пакет и опубликованный скрипт перехода совпали с GitHub SHA-256; UI/хост внутри пакета совпали с `micnoize-bundle.json`. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.9.

## 2026-09-25 — сохранение пакета перед загрузкой обновления (выпущено в 0.2.9)

- Загрузка 0.2.8 через Velopack удалила полный пакет установленной 0.2.7 из `packages`, поэтому подготовка безопасного отката остановила обновление. На этом ПК восстановлен опубликованный пакет 0.2.7 с SHA-256 `aa1fa6f697fb430cdafa2d36543cb2ad392d0df2dfa360f749db4251bfb9688e`; хеши его UI и хоста совпали с установленными. TAG-хост не останавливался.
- Перед будущими загрузками приложение сохраняет проверенный пакет текущей версии вне кэша Velopack; подготовка обновления использует эту копию, если Velopack удалил исходный файл.
- Release workflow теперь восстанавливает предыдущий Rust-кэш того же компилятора при изменении `Cargo.lock`: раньше каждый патч менял версию пакета и вызывал полный промах кэша. Время следующего релиза ещё не измерено.
- Для быстрого релиза добавлен двухэтапный `scripts/release-local.ps1`: локальные проверки/сборка, затем отдельная публикация через черновик GitHub Release со сверкой SHA-256 каждого загруженного файла. Требуются чистый коммит, совпадение с публичной веткой и локально заданный секрет телеметрии. Push тега больше не запускает повторную облачную сборку; ручной GitHub workflow сохранён как запасной путь.
- На прогретой локальной сборке измерено: `verify.ps1` — 69,0 с; `cargo build --release --locked --offline` — 48,6 с. Упаковка/загрузка и полный локальный релиз с настоящим секретом ещё не измерены.
- Прошло: `cargo test --release --locked --offline update_download_retains_current_package_outside_velopack_cache` — 1 passed; `scripts/check-release-local.ps1` отклоняет изменённый файл до обращения к GitHub; PowerShell-синтаксис новых скриптов и `git diff --check` без ошибок. После восстановления опубликованного пакета обычное обновление 0.2.7 → 0.2.8 завершилось: журнал `Complete`, UI и хост совпадают с хешами опубликованного пакета, оба процесса хоста запущены. Откат 0.2.8 не проверялся.

## 2026-09-25 — исправления отрисовки нового дизайна (после 0.2.8)

- На установленной 0.2.8 (100 % масштаб) найдено: сегменты ползунков склеивались из полосок по 2 px, на стыках сглаживание давало горизонтальную «зернистость»; свечение края было прямоугольной тенью, торчащей над и под скошенным сегментом; у слова реверса и центрированных/правых подписей оставались обрывки старых кадров, потому что tiny-skia перерисовывает только область, которую отсчитывает от точки привязки текста.
- Скошенные сегменты, бирка значения и полосы ленты «экспериментально» теперь рисуются сглаженными путями (`iced_tiny_skia` с функцией `geometry`, добавлены `lyon_path`, `lyon_geom`, `euclid`); неизменная геометрия кэшируется. Свечение — кольца со скруглёнными углами по форме сегмента. Пустые сегменты — тёмная заливка с рамкой, как в макете. Собственный текст рисуется от измеренного левого верхнего угла; подписи dB выровнены контейнером. У последней записи — условная волна из 30 столбиков вместо плоской полосы.
- Прошло: `cargo test --release --locked` — 56 passed, 2 ignored; `cargo clippy --all-targets -- -D warnings` чисто; снимки в `results/design-0.2.9`; тест прокрутки саундпада держит медиану ≤4 областей перерисовки.
- Не проверено: исправленная сборка в живом окне (анимация реверса, свечение во время волны), масштаб 125–200 %, нагрузка на CPU.

## 2026-09-25 — релиз v0.2.8 опубликован

- Первый запуск `release.yml` (run `36083264925`) упал на clippy 1.98 (`chunks_exact_to_as_chunks` в тесте снимков); исправлено. Публичный коммит `7e1b160a0cdbe7b836e15dd19aec4ca213470734`; GitHub Actions run `36084041937`: native build, проверки, упаковка и публикация прошли. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.8.
- `Setup.exe`: 25,078,067 байт, SHA-256 `964598b7b8a82ad9ce596438ff33f247fdb3132209b194c381393efe40ea64d5`. Полный пакет: 20,616,499 байт, SHA-256 `065f9e7c0fc83005840ffb76f7b1f02a8894456afa3cdaca3bdfe190c93d5bae`. `MicNoize-Upgrade-0.2.8.zip`: 38,927,586 байт, SHA-256 `3357ad92c53c89248c0169972307d57bdafde1146c826bda7b5426654c26b2ac`.
- Не проверены: новое окно вживую (масштаб 100–200 %, мышь/клавиатура), нагрузка анимаций на CPU. Обновление установленной 0.2.7 до 0.2.8 прошло после восстановления удалённого Velopack пакета 0.2.7 (описано выше).

## 2026-09-25 — патч 0.2.8: новый дизайн UI

- Rust UI перерисован по согласованному макету: своя строка заголовка, меню слева (Шумодав, Эффекты, Саундпад, Смена голоса, Настройки), карточки, кейкапы хоткеев, переключатели вместо галочек, значки Segoe MDL2 и цифры Bahnschrift (системные шрифты, новых зависимостей нет). Окно 1040×740, минимум 960×680.
- Новый ползунок «Тахометр» (`ui/src/tacho.rs`): скошенные сегменты, красная зона, знаковое и зеркальное заполнение, Ctrl+клик — сброс; анимации (волна в простое, прогрев, бегущие цифры, вспышка и «призрак» края) идут только в активном окне. «Сила» выше 100 %: предупреждение и лента «экспериментально».
- «Наушники» переехали в панель по шестерёнке на странице «Шумодав»; маршрут «Обработка и выход» свёрнут; индикатор «До / После» берёт уже существующий `input_peak`. Эффекты — отдельная страница с тренировочным словом реверса; «Повтор последнего» — крупная карточка и пять плиток с кнопкой сохранения на каждой. «Логи» открываются из «Настроек». Порядок Tab и Esc обновлён под новые страницы.
- Прошло: `cargo test --release --locked --offline` — 55 passed, 0 failed, 2 ignored; `cargo clippy --all-targets -- -D warnings` чисто; headless-снимки tiny-skia всех страниц в `results/design-0.2.8`.
- Не проверено: живое окно (установленный UI держит single-instance), анимации и их нагрузка на CPU, масштаб 100–200 %, мышь/клавиатура вживую, трей. Не сделано: меню трея и новая иконка в новом стиле, настройка «Анимации». Версия 0.2.8 в `release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock`; пользовательский текст — `release/notes.md`.

## 2026-09-25 — патч 0.2.7: пакет для следующего отката

- На обычной установленной 0.2.5 после перехода на 0.2.6 обнаружено, что Velopack очистил `packages`: новый UI/хост работали, но следующему обновлению не хватило бы полного пакета предыдущей версии. На проверяемом ПК полный пакет 0.2.6 сохранён в `packages` с проверкой опубликованного SHA-256; приложение и endpoint не перезапускались.
- После успешного перехода с 0.2.5 и обычного обновления проверенный полный пакет установленной версии атомарно сохраняется в `packages` перед завершением журнала. Повтор не создаёт дубликат и отклоняет повреждённую копию. Исторический релиз 0.2.6 остаётся доступен; пользователи, уже перешедшие через его Upgrade ZIP, могут один раз поместить полный пакет 0.2.6 из релиза в `packages` перед обычным обновлением.
- GitHub Actions run `36079273371` прошёл native/Rust проверки, упаковку и публикацию публичного коммита `576bf37`; релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.7. Полный пакет: 20,572,730 байт, SHA-256 `aa1fa6f697fb430cdafa2d36543cb2ad392d0df2dfa360f749db4251bfb9688e`.
- На этом ПК обычная установленная 0.2.6 обновлена из точно опубликованного полного пакета 0.2.7. Журнал завершён, UI/хост соответствуют манифесту, полный пакет 0.2.7 сохранён в `packages`; задача и автозапуск указывают на `%LOCALAPPDATA%\MicNoize\current`. Линия 3 и endpoint `{0.0.1.00000000}.{e4ba2cb9-afc9-47ed-909f-20513051c269}` сохранились, хост готов; монопольный захват запрещён, два общих клиента получили по 480 кадров. Проверка перехода с 0.2.5 и отката настоящего пакета выполнялась на 0.2.6; именно переход 0.2.5→0.2.7, повторная перезагрузка и откат 0.2.7 на этом ПК не выполнялись.

## 2026-09-25 — релиз v0.2.6 опубликован

- Публичный коммит `7037716f519fcad323a081f65015d7a877ca3577`; GitHub Actions run `36076052036`: native build, проверки, упаковка и публикация прошли. Релиз: https://github.com/Arkanoidvfx/MicNoize/releases/tag/v0.2.6.
- `Setup.exe`: 25,030,988 байт, SHA-256 `d9e6cbc794945e092a4f40aac9c3b9ef3d4b5d5c1a2270d6778bfed3c94bdec5`. `MicNoize-Upgrade-0.2.6.zip`: 38,833,396 байт, SHA-256 `5a4d28e36d83cc2208e1c1fdc9f3aceed5e8fe2afccc39fe4a723de362ef7962`. Старый канал 0.2.5 не содержит этот пакет; переход требует отдельный Upgrade ZIP.
- Не проверены сон, физическое переподключение микрофона, повторное открытие потока Discord и чистая установка; отключение питания во время обновления проверялось только имитацией состояния файлов.

## 2026-09-25 — патч 0.2.6: надёжность виртуального микрофона

- Хост сохраняет линию и имя Mic Noize, удерживает громкость Windows на 100%, снимает системный Mute и остаётся подключённым после выхода из UI. Запуск через Планировщик и восстановление после сбоев ограничены по числу попыток. Виртуальное устройство работает в общем режиме; монопольный захват запрещён.
- «Обновить устройства» проверяет хост и безопасно восстанавливает соединение. Изменение линий/драйвера выполняется через явный ремонт. UI/хост поставляются одной версией с журналом обновления и откатом; для опубликованной 0.2.5 нужен отдельный `MicNoize-Upgrade-0.2.6.zip`.
- На установленном комплекте и после реальной перезагрузки прошли имя/endpoint/100%/Mute, два shared-клиента, синтетический сигнал и совместный тест наушников. На копии опубликованного 0.2.5 прошли переход с работающим старым хостом и откат. Прерывания обновления проверены имитацией. Rust 51 passed / 1 ignored, native CTest 3/3, строгий Clippy; подробные результаты ниже.
- Сон, переподключение физического микрофона, повторное открытие Discord и чистая установка не проверены. Реальное отключение питания не имитировалось; гибернация на тестовом ПК отключена.

## 2026-09-25 — ранний запуск и кандидат перехода с 0.2.5 (не выпущен)

- Установленная рабочая пара: UI 0.2.5 / host 2026092411. Ранний Stop до публикации endpoint, усыновление инициализирующегося worker после потери supervisor (120 с), первый ремонт без задачи Планировщика прошли; исходный endpoint сохранён. `tag-startup-retry-install.log`. Снимки настроек 100%/200% просмотрены: состояние «Готово», элементы доступны и не обрезаны.
- Полная проверка build 11 при закрытом UI: три аварийных восстановления 61.8/62.1/61.7 с, потеря обоих процессов 120.8 с, потеря supervisor во время backoff 117.5 с. Исчерпанный общий бюджет и отмена устаревших поколений проверены (`tag-full-recovery-11.log`).
- Исходники кандидата 0.2.6 / host 2026092501: отдельный переход со старого пакета, сохранение core-хоста/пакета/Run до замены, проверка установленной версии вместе с хешами пары, журнал восстановления legacy. Упаковка разделяет каналы: первый переход не обслуживается старым автоматическим обновлятором. Ничего не опубликовано.
- На копии настоящего опубликованного пакета 0.2.5 переход завершился целой парой 0.2.6, endpoint линии 3 сохранён, старый пакет и core-хост сохранены, политика shared-only восстановилась за 31 мс, два клиента получили по 480 кадров. Старый хост в этом прогоне не запускался. После проверки возвращены рабочая пара 11, обе задачи и настройки (`tag-legacy-upgrade.log`).
- Native 3/3, Rust 51 passed / 1 ignored, Clippy `-D warnings`, сборка прошли. Новые проверки отвергают старую `sq.version` при совпадающих файлах, а также небезопасный набор линий для legacy-отката. Это не проверка отключения питания.
- Проверка несовместимого кандидата выявила преждевременный запуск старого UI самим Update.exe. Добавлен явный `--norestart`: порядок восстановления и запуска теперь определяет наш recovery. Следующий прогон выявил различие разделителей в путях Windows, из-за которого свой старый хост считался чужим; пути нормализованы в общей проверке. Повторный откат восстановил точный опубликованный UI/core и проверил ответ v1/собственный endpoint до запуска UI; TAG-линии и Run сохранены (`tag-legacy-rollback-path.log`).
- Уточнена идентичность baseline: опубликованный core с хешем `0D196E…BC726` собран до изменения `9217dd1`, удаляющего стандартные линии; этого удаления в нём нет (сверены история, бинарник и фактический набор линий). Предварительное предположение по более позднему HEAD к нему неприменимо. Проверка legacy-перезапуска допускает TAG defaults, но требует уникальную собственную capture-линию и отвергает старые `MicNoiseReducer*`, которые этот core удаляет.
- Последняя пара UI 0.2.6 / host 2026092501 прошла переход с реально работающего старого core без смены линий (`tag-legacy-live-upgrade.log`). При исчезновении всей папки `current` Velopack требовал `sq.version`; восстановление теперь извлекает только проверенные метаданные из сохранённого пакета и затем применяет полный пакет. Имитации прерывания Prepared, Applying с отсутствующим каталогом и Complete прошли (`tag-legacy-cold-*.log`). Это имитации состояния файлов/журнала, не отключение питания.
- Финальная пара установлена локально; предыдущие UI/хост/probe, обе задачи и настройки сохранены в `build/tag-release-candidate-rollback-20260925`. Точный endpoint линии 3 прежний; shared-policy восстановилась за 16 мс, exclusive отклонён, два клиента получили по 480 кадров. Master/два канала/Mute прошли; одновременно микрофон и наушники — 300 стереоблоков, 0 drops, защита от обратной связи по идентичности. Синтетический микрофон: 76,800 кадров, peak 0.1, RMS 0.0707107, discontinuities 0 (`tag-final-candidate-install.log`).
- Итоговые Rust 51 passed / 1 ignored и Clippy без замечаний; релизный пакет пересобран, хеши UI/хоста сверены внутри nupkg и внешнего Upgrade ZIP. Setup 24,975,667 байт; полный пакет 20,514,099; Upgrade ZIP 38,742,024. Публичного выпуска нет. Перезагрузка после последних изменений, сон, чистая установка и внешние приложения остаются условиями приёмки.
- Финальный обычный парный update/rollback и восстановление без всего каталога `current` прошли (`tag-final-paired-transaction.log`). Стенд использовал тот же финальный UI 0.2.6 с тестовым номером пакета 0.2.7; версии 0.2.7 не выпускалось. После проверки возвращены установленный кандидат, обе задачи, настройки и Run.
- Реальная перезагрузка 25.09 в 01:57 пройдена без Refresh/ремонта: прежние хеши пары, линия микрофона 3 и наушников 4, точный endpoint, имя Mic Noize, задача и автозапуск UI. Ready=1, master 100%, системный Mute выключен; exclusive отклонён, два shared-клиента получили по 480 кадров, контроль уровня восстановил master/каналы/Mute. Наушники после загрузки: 300 синтетических стереоблоков, 0 drops, защита от петли по идентичности. До/после — `tag-before-reboot-final-20260925.json`, `tag-after-reboot-final-20260925.json` и соседние endpoint-снимки. Сон, USB-переподключение, Discord и чистая установка ещё не проверены.

## 2026-09-24 — ремонт линий, состояния и зависание наблюдателя

- Build 09: явный ремонт освобождает слот для наушников, удаляя только проверенную стандартную capture-линию TAG; согласие сохраняется для следующих загрузок. Два ремонта через UI сохранили рабочую линию 3 и точный endpoint. Микрофон и наушники одновременно: 300 stereo-блоков, 0 drops; синтетический микрофонный сигнал без discontinuities. Обратная связь блокируется по идентичности драйвера/KS, независимо от имени (`tag-line-repair-install.log`).
- Простой build 09: за 65.09 с подтверждённого отсутствия слушателей счётчик CPU постоянной пары процессов не показал прироста, private memory 4,288,512 байт. Это ниже разрешения счётчика, а не доказательство нулевой нагрузки; короткий внешний процесс проверки отдельно не измерялся (`tag-line-repair-idle.json`).
- Установлен build 10: ранняя идентичность аудиопроцесса, heartbeat наблюдателя, шесть состояний в настройках. Реальное приостановленное выполнение всех потоков наблюдателя восстановлено за 179.6 с без смены рабочего аудиопроцесса; обоих процессов — за 256.8 с. UI был закрыт, слушателей не было, точный endpoint сохранился (`tag-state-install.log`). Это отличается от проверки завершения процесса.
- На build 10: политика shared-only восстановлена за 16 мс; exclusive отклонён, два shared-клиента получили по 480 кадров. Синтетический сигнал: 78,240 кадров, peak 0.1, RMS 0.0707107, усиление драйвера 31.6228, discontinuities 0. Native 3/3, Rust 49 passed / 1 ignored, Clippy без замечаний. Настройки визуально проверены при 100% и 200% (`tag-state-100.png`, `tag-state-200.png`).
- В исходниках следующего кандидата добавлено сохранение старого пакета, core-хоста и двух значений автозапуска до очистки Velopack; проверка удаления исходного пакета/повтора/повреждения резервной копии прошла (Rust 50 passed / 1 ignored). Полная миграция старой установки и её откат ещё не реализованы; эти исходники не установлены. Также найдены и исправляются Stop во время ранней инициализации и восстановление состояния отсутствующей задачи после первого ремонта.
- Полный выпуск не принят: загрузка Windows после новых изменений ремонта/фоновой задачи, сон, физическое переподключение, внешние приложения, чистая установка и переход с опубликованной версии остаются отдельными проверками.

## 2026-09-24 — восстановление после потери обоих процессов

- Build 08: короткая задача Планировщика проверяет хост раз в минуту. Поколение операции, текущий вход Windows и общий лимит трёх попыток сохраняются в реестре; Stop отменяет отложенные запуски. Задача имеет `Volatile=true`, без запуска при выключенном автозапуске после следующей загрузки Windows.
- Реальная проверка с закрытым UI: parent/worker/parent — 61.5/61.4/61.4 с; оба процесса потеряны — 121.4 с; parent погиб во время задержки перезапуска worker — 118.4 с. Четвёртая авария не сбросила лимит даже после потери обоих процессов (130 с наблюдения). Stop отклонил старое поколение worker, запоздавший запуск parent и ожидающее восстановление. Endpoint сохранился. `results/tag-durable-install-retry.log`.
- Общий режим: два клиента по 480 кадров, exclusive отклонён, политика восстановлена за 15 мс. Синтетический сигнал: 78 240 кадров, peak 0.1, RMS 0.0707107, без разрывов. Native 3/3, Rust 48 прошли / 1 ignored, Clippy без предупреждений.
- Проверка `Volatile` после реальной загрузки Windows, сон S3, старый опубликованный пакет и полный выпуск пока не приняты. Гибернация на этом компьютере отключена; она не включалась для проверки.

## 2026-09-24 — восстановление наблюдателя и выход из трея

- Build 2026092407: аудиопроцесс переживает падение наблюдателя и через 60 секунд бодрствования возвращает его через Планировщик. Наблюдатель принимает прежний worker только после проверки пути, PID/времени создания и сеанса. Удалён kill-on-job-close; штатная остановка остаётся кооперативной, с принудительным завершением только проверенного worker при зависании.
- Общий лимит трёх повторов хранится в семафоре, который наследует только worker через явный список handle. Перезапуск любого одного процесса не сбрасывает лимит. При его исчерпании живой worker продолжает обслуживать endpoint, но потеря наблюдателя пока отражается только в журнале.
- Реальный тест при закрытом UI: аварии наблюдатель/worker/наблюдатель восстановились за 61,9/62,1/62,6 с; при аварии наблюдателя worker и endpoint сохранялись. Четвёртая авария не вызвала повтор за 70 с; штатный Stop во время ожидания отменил повтор ещё на 70 с. Финальный хост запущен с новым лимитом. Проверка — `scripts/check-tag-recovery.ps1`, лог — `results/tag-parent-install.log`.
- Повторно прошли master/каналы/Mute, запрет exclusive (восстановление политики 16 мс), два shared-клиента. Синтетический сигнал: 77 760 кадров, пик 0.1, RMS 0.0707107, gain 31.6228, discontinuities 0. Endpoint прежний. Предыдущая пара сохранена в `build/tag-parent-rollback-20260924-retry`.
- Установлена причина долгого выхода из `--start-tray`: после освобождения native-движка Winit 0.30.13 входил в ожидание сообщений после запроса выхода из AboutToWait. Отправка WM_NULL пробуждала его и немедленно завершала процесс; воспроизведено без NVIDIA-обработки и с ней. Общий `exit_ui()` теперь ставит WM_QUIT перед обычным `iced::exit()`. Выход после исправления — 423/426 мс, хост не перезапускался (`tag-tray-exit-before-fix.log`, `tag-tray-exit-check.log`). Добавлены ограниченные существующим logger этапы завершения и отдельная проверка `scripts/check-tag-tray-exit.ps1`.
- Проверки: 3/3 native CTest; 48 Rust passed / 1 ignored; строгий Clippy, Release-сборки и diff check. Отдельный tray regression прошёл за 302 мс. Изолированные задачи с выходом 1, -1 и HRESULT E_FAIL не получили ни одного повтора за 90 с; временные задачи удалены. Полный план остаётся открытым: одновременная потеря обоих процессов, падение наблюдателя до создания worker/между попытками, миграция старых пакетов/линий, чистая установка и аппаратные сценарии не приняты. Финальное состояние — `results/tag-parent-final-state.json`; UI открыт, хост ready=1. Публичного выпуска нет.

## 2026-09-24 — повторная перезагрузка: идентичность сохранена

- Windows загрузилась в 21:22:17; до Refresh, ремонта или перезапуска проверены установленная пара build 2026092406 и её автозапуск. UI стартовал с `--start-tray`, задача запустила supervisor/worker, ready=1. Хеши обоих EXE совпали с исходным снимком.
- Линия 3 и точный endpoint `{0.0.1.00000000}.{e4ba2cb9-afc9-47ed-909f-20513051c269}` сохранились. Имя Mic Noize (Thin Audio Gateway), master 100%, системный Mute выключен; сохранённые ID 3/4 на месте. Задача Running, без лимита времени, зависимости от сети/простоя и остановки на батарее.
- Функциональная проверка после загрузки: exclusive отклонён с `0x8889000E`, два одновременных shared-клиента получили по 480 кадров. Аудио не сохранялось; измерение амплитуды и изменение уровней в этом прогоне не выполнялись. Снимок `results/tag-after-reboot-identity-20260924.json`, endpoints и shared-проверка — соседние `tag-after-reboot-identity-*.log`.
- Проверка этой перезагрузки пройдена. Сон/гибернация, чистая установка, миграция старых пакетов/линий, авария родительского наблюдателя и внешние приложения остаются открытыми; полный план завершённым не объявляется. Изменена только документация, сборка и перезапуск не требовались.

## 2026-09-24 — проверка перезагрузки и сохранение ID линии

- До вмешательства проверена загрузка Windows в 21:01:23: UI и планируемый хост build 2026092405 стартовали сами, ready=1, имя Mic Noize (Thin Audio Gateway), уровень 100%, Mute выключен. Exclusive отклонён с 0x8889000E, два shared-клиента получили кадры. Но endpoint изменился: драйвер восстановил стандартные линии 1/2, а хост выбрал новую линию 3. Исходные снимки — `results/tag-before-reboot-20260924.json` и `tag-after-reboot-20260924.json`.
- Build 2026092406 сохраняет ID обеих линий в HKCU до аудиоцикла, повторно использует их и отклоняет конфликт вместо дальнейшей смены идентичности. Начальное выделение пропускает зарезервированные драйвером 1/2. Порядок перечисления, отображаемое имя и большие ID чужих линий не сдвигают выбор. Native check покрывает эти случаи, дубликаты и конфликт сохранённого ID.
- 3/3 native CTest, native/host/Rust Release и diff check прошли. Пара установлена согласованно; сохранены ID 3/4. Повторный запуск хоста сохранил текущий endpoint. При закрытом UI прошли master/каналы/Mute, восстановление exclusive-policy за 31 мс, отказ exclusive и два shared-клиента. Синтетический сигнал: 77 280 кадров, выходной пик 0.1, RMS 0.0707107, gain 31.6228, discontinuities 0; речь не записывалась. Лог `results/tag-identity-install.log`, предыдущая пара — `build/tag-identity-rollback-20260924-retry`.
- Первая попытка замены безопасно остановилась: UI не завершился за 15 секунд, файлы не заменялись. Позже процесс завершился; повторная штатная остановка прошла. Причина задержки выхода не установлена, исправление продукта для неё не заявляется.
- Новый снимок `results/tag-before-reboot-identity-20260924.json` подготовлен для следующей перезагрузки. Сохранение endpoint после неё пока НЕ проверено. Старые установки с собственной линией 1/2 требуют отдельной миграции; лимит трёх линий demo-драйвера, чистая установка, сон, авария родительского наблюдателя и остальные открытые пункты не приняты. Публичного выпуска нет.

## 2026-09-24 — общий режим Mic Noize, запрет монопольного захвата

- По решению пользователя Mic Noize поддерживает только общий WASAPI. Требование паритета shared/exclusive заменено запретом exclusive; компенсация -30 дБ для TAG 2.0.0.1903 остаётся. Драйвер не исправлен и не заменён.
- Хост build 2026092405 применяет Windows exclusive-control policy только к endpoint, подтверждённому через драйвер/KS. Проверка при привязке, уведомлениях и раз в секунду выполняется вне аудиоцикла, без постоянного захвата. Запись только при отличающемся значении, Commit и повторное чтение; неподдерживаемый тип/ошибка снимают готовность и оставляют тишину. Свойство не входит в публичный Windows SDK: текущая система проверена, переносимость на другие версии не заявляется.
- Добавлены явные проверки `mic_tag_probe --check-shared-only` и `--check-shared-guard`. Второй тест временно включает exclusive только у подтверждённого виртуального endpoint и проверяет восстановление хостом; при ошибке возвращает запрет. Инициализация exclusive обязана вернуть именно `0x8889000E`, обычная занятость не считается успехом.
- На установленной сборке, UI закрыт: политика восстановлена за 31 мс; exclusive отклонён; два одновременных shared-клиента получили по 480 кадров; master/два канала/Mute восстановлены. Синтетический shared-сигнал: 78 240 кадров, входной пик 0.00316228, выходной 0.1, RMS 0.0707107, gain 31.6228, discontinuities 0. Физический микрофон тестом не открывался, аудио не сохранялось. Лог `results/tag-shared-install.log`.
- 3/3 native CTest, native/host/Rust Release прошли. Локальная пара обновлена согласованно, endpoint сохранён, UI открыт. Предыдущая пара — `build/tag-shared-rollback-20260924`. Реальная перезагрузка, сон, чистая установка, старый опубликованный пакет и авария наблюдателя по-прежнему требуют приёмки; публичного выпуска нет.

## 2026-09-24 — проверенный откат и исправление команд фонового хоста

- Build 2026092404: команды WinMain разбираются через `CommandLineToArgvW`. Подтверждён реальный сбой Windows PowerShell 5.1: `Start-Process` добавляет конечный пробел, из-за которого прежнее буквальное сравнение отклоняло `--stop`/`--task-start`. Native checks покрывают пробелы, кавычки и лишние аргументы; ошибки SetEvent журналируются.
- При откате помощники Update/Recovery удерживали заменяемую папку `current` как рабочую: Velopack получал sharing violation и оставлял новый комплект. Теперь их рабочая папка находится вне `current`; сохранённый Recovery также меняет cwd при запуске. Дефект воспроизведён полным тестом и устранён повторным прогоном (`tag-rollback-cwd-failure.log`, `tag-full-transaction.log`).
- Готовность проверяется после замены и после отката, с задержками 2/5/15/30/60 для временных ошибок. Доступ/несовместимость/отключённое устройство не принимаются за поздний Windows Audio. Обслуживание отклоняется при владельце TAG в другом сеансе. Поиск endpoint учитывает состояние Disabled; проверка реального отключения в Windows ещё нужна.
- Удалено автоматическое удаление стандартных TAG-линий и его файловый logger из `TagOutput`. При заполнении трёх линий demo-драйвера требуется явное решение; обычный запуск не удаляет другие устройства. Чистая установка с заполненным лимитом не принята.
- Реальные UI 0.2.5/0.2.6 и хост в отдельной portable-установке, реальная текущая задача Windows и TAG: обновление прошло; несовместимый хост вызвал ровно один откат; смешанный комплект и отсутствующий UI восстановились из сохранённого пакета, каждый с одним откатом. Во всех случаях endpoint/line сохранились и host ready=1. После теста исходные задача, Run, настройки и хост восстановлены. Это искусственно подготовленные состояния диска, а не отключение питания. Лог `results/tag-full-transaction.log`; сценарий `.tmp/check-full-transaction.ps1`.
- Локально установлены UI 0.2.5 и host build 2026092404; предыдущая пара сохранена в `build/tag-transaction-rollback-20260924`. После замены ready=1, master/два канала/Mute восстановлены при закрытом UI; UI снова открыт (`tag-final-install.log`).
- Проверки: 3/3 CTest, 48 Rust passed / 1 ignored, строгий Clippy и обе Release-сборки прошли (`results/tag-final-*`). Проверка пакета доступна только с `--ui-benchmark --check-update-package` и путём внутри packages отдельной Velopack-установки.
- Выпуск остаётся заблокирован: TAG 2.0.0.1903 отклоняет диапазон громкости; shared/exclusive дают разное усиление, компенсация сохранена. Холодная загрузка/сон/чистая установка/внешний Discord, миграция старого опубликованного пакета без bundle и авария родительского наблюдателя ещё не приняты. Никаких реальных отключений питания или переустановки драйвера в этом прогоне не было.

## 2026-09-24 — ремонт устройства и подготовка отката комплекта

- Хост build 2026092403 поставляется рядом с UI; пакет содержит версии и SHA-256 обоих EXE. Загрузка NVIDIA core больше не заменяет актуальный хост. Подготовка обновления проверяет и сохраняет оба полных пакета в фоне до остановки аудио. Отсутствующий предыдущий пакет и старый пакет без нового manifest останавливают применение.
- Добавлен журнал `.update` вне `current`: сохранённые пакеты/установщик/процесс восстановления, этапы замены, приостановка задачи, проверка готовности и ограниченный откат. Восстановление запускается после завершения установщика и при следующем входе в Windows. Намерение микрофона, наушников и мониторинга сохраняется; ошибка применения позволяет продолжить прежнюю обработку. Это ещё не принятый для выпуска сценарий: полная реальная миграция и прерывание каждого этапа не проверены.
- «Восстановить устройство» требует подтверждения и отдельно разрешённой переустановки. Исправная линия используется повторно; прерванный ремонт не повторяет UAC автоматически. Выход во время ремонта запрещает возобновление звука. Установка драйвера на новом ПК вызывается кнопкой. Refresh и ремонт доступны с клавиатуры при работающем маршруте.
- Исправлены остановка UI-таймера при переходе к асинхронному ремонту/применению обновления и опасный фиксированный буфер в диагностике отказа создания TAG-линии. Отсутствующий драйвер/runtime повторяется с задержками 2/5/15/30/60 секунд, отдельно от трёх аварийных повторов.
- Проверки: native CTest 3/3; Rust 47 passed, 1 ignored; строгий Clippy и Release прошли. Проверены повреждённый/смешанный комплект, этапы и частично записанный журнал, сохранённое намерение, ошибки подготовки, возврат управляющего потока и клавиатура. Реальный сохранённый Velopack Update.exe установил полный тестовый комплект 2.0.0 и вернул 1.0.0 в отдельной portable-папке; хеши совпали. Это не тест отключения питания или реального UI/host-обновления.
- Локальная пара установлена, endpoint остался прежним, host ready=1. Master/оба канала/Mute восстановлены при закрытом UI. Ремонт здорового устройства через настоящий UI обработан без смены endpoint; таймер продолжает работать. Окно подтверждения осмотрено при 100/200%. За 20 секунд при закрытом UI процессный счётчик CPU пары не вырос в пределах своей точности; отсутствие стороннего слушателя независимо не подтверждено. Предыдущая пара: `build/tag-maintenance-rollback-20260924`.
- Полный план НЕ завершён: блокер подписанного драйвера (+30 дБ/shared против exclusive), имя после загрузки, чистая установка, полная миграция/откат с прерыванием, сон/гибернация, падение наблюдателя и внешние приложения остаются непринятыми. Дополнительные замечания ревью и точные границы проверок — `results/tag-maintenance-review.md`; текущие логи — `results/tag-maintenance-*`, `tag-velopack-pair-check.log`, снимки `tag-repair-*.png`. Публичного выпуска нет.



## 2026-09-24 — IPC v2, фоновое восстановление и рабочий Refresh

- Микрофон, наушники и снимок готовности переведены на отдельные `.v2` объекты; старый v1 не переопределяется. Проверяются GUID хоста/соединения, номера, deadline, версия/размер, границы и конечность сэмплов. Тайм-аут делает соединение недействительным. Проверка живости читает отдельный снимок, без аудиокоманд и аудиоблокировки. PE-ресурс хоста позволяет проверить совместимость без выполнения файла.
- Автозапуск хоста перенесён в задачу текущего пользователя: интерактивный сеанс, без повышения прав, временного лимита, условий сети/простоя/батареи; IgnoreNew, RestartCount=3 и PT1M. Предпочтение входа сохраняется, прежний Run удаляется после проверки задачи. При недоступности Планировщика предусмотрен прямой запуск с предупреждением.
- На этом Windows `RestartOnFailure` не восстановил процесс: подтверждено изолированными задачами с exit 1, с UnifiedSchedulingEngine и без него. Поэтому задача запускает наблюдателя с принадлежащим ему аудиопроцессом в Job Object. Падение/зависание аудиопроцесса: до трёх повторов через минуту; watchdog 15 секунд по времени без сна. Чужие процессы не завершаются. Общая блокировка защищает драйвер от второго сеанса.
- Refresh перечитывает устройства и проверяет хост при любом состоянии маршрута; исправный маршрут сохраняется, незавершённый запуск отменяется через поколение. Запоздалые Started отбрасываются; выход отменяет очередь запусков. Запросы перечитывания объединяются. Уведомления устройств и пробуждение обновляют список. Успешный запуск очищает прежнюю ошибку; переподключение TAG сбрасывает старые эффекты/очередь.
- Проверено с закрытым UI: принудительное падение аудиопроцесса → готовность через 60 секунд; полная приостановка его потоков → watchdog и восстановление примерно за 71 секунду по циклу проверки; второй/третий повторы успешны, четвёртая ошибка исчерпывает лимит, более 70 секунд повторов нет. Штатная остановка не перезапускается спустя 68 секунд. Endpoint остался прежним. Логи `results/tag-scheduler-live.log`, `tag-supervisor-live.log`, `task-restart-comparison.log`.
- Одновременные микрофон/наушники: 479 520 кадров микрофона, 300 стереоблоков наушников; underruns/drops/TAG gaps 0, capture discontinuities 3, lateTicks 1. Producer stalls 120/400 мс пережиты с двумя переподключениями, без потерь TAG. Shared-синус: 77 760 кадров, peak 0.1, RMS 0.0707107. Речь не сохранялась. Логи `results/tag-v2-*`.
- Последняя сборка: 3/3 native CTest, 19 Rust controller tests, Rust Release прошли. Установлена согласованная пара; контроль master/двух каналов/Mute при закрытом UI прошёл, UI снова открыт, host ready=1. Предыдущая пара в `build/tag-refresh-rollback-20260924`; это локальная страховка, не транзакционный механизм обновления. Логи `results/tag-refresh-*`.
- План НЕ завершён. Остаются блокер 0 дБ/shared-exclusive, автоматическое имя после перезагрузки/чистая установка, полноценный ремонт, отдельная доставка хоста и устойчивый к прерыванию откат всего комплекта. Не подтверждены падение самого наблюдателя, сон/гибернация, второй сеанс, физические сценарии и повторное открытие Discord. Публичного выпуска нет.

## 2026-09-24 — постоянный контроль уровня и точная привязка TAG

- Установлена локально независимая от блокера драйвера часть: поток хоста находит endpoint по экземпляру драйвера и KS-топологии, удерживает master/каналы на 100 %, снимает системный Mute по уведомлениям и раз в секунду, проверяет результат. COM и журнал вне аудиоцикла; собственные изменения помечены GUID. Движок/монитор используют опубликованный endpoint и компенсацию; при недостоверном состоянии подаётся тишина.
- Добавлен отдельный снимок `TagEndpoint.v1` с PID/временем запуска и свежестью проверки; audio v1 не изменён. Убраны подстановка чужой линии и автоматическое удаление линий прежнего MicNoiseReducer. Дубликаты своей линии — ошибка. Некорректный формат клиента отклоняется без завершения хоста. Refresh сбрасывает прежнюю ошибку/задержку; распознанные временные ошибки продолжают повторяться раз в 60 с после начальных 2/5/15/30/60 с.
- Проверено на новом хосте с закрытым UI: готовность без слушателя, тот же endpoint, возврат master и двух каналов на 100 % и снятие Mute в пределах секундного окна теста. Shared-синус: 78 240 кадров, peak 0.1, RMS 0.0707107, discontinuities 0. За 10 с без слушателя CPU вырос на 0 мс по счётчику процесса (его разрешение ограничено).
- Native self-test и 17 Rust controller tests прошли; host/UI собраны и установлены парой, предыдущие exe сохранены в `build/tag-rollback-20260924`. Реальный HyperX → NVIDIA → TAG: 144 960 принятых кадров, underruns/drops/TAG gaps 0, capture discontinuities 1. Задержки производителя 120/400 мс: два переподключения, та же сессия/хост, без underruns/drops/TAG gaps; capture discontinuities 3/5. Речь не сохранялась. Логи: `results/tag-control-*`.
- Полный план не завершён: диапазон 0 дБ и равная громкость exclusive заблокированы; audio IPC с поколениями/номерами запросов, Планировщик, восстановление после падения с закрытым UI, ремонт и транзакционное обновление/откат ещё не реализованы. Сохранение пары exe — только локальная страховка, не приёмка транзакционного отката. Перезагрузка/сон/чистая установка, повторное открытие Discord и общая матрица отказов не проверялись. Публичного выпуска нет.

## 2026-09-24 — блокер первого этапа плана надёжности TAG

- Добавлен отдельный `mic_tag_probe`: чтение endpoint/KS-топологии, списка линий, проверка `SetVolumeControlParams` с восстановлением параметров и измерение синтетического сигнала через shared/exclusive WASAPI. Не запускается из приложения или обычного CTest; не создаёт линии, не завершает процессы, не открывает физический микрофон.
- Подписанный TAG 2.0.0.1903 отклонил и диапазон -96…0 дБ, и исходные параметры с `0x80070001`. Состояние драйвера не изменилось. Windows по-прежнему сообщает максимум +30 дБ, scalar 1 и отсутствие аппаратной регулировки.
- Измерение реального capture-пути: сигнал с пиком 0.00316228 принят как 0.1 в shared (усиление 31.6228) и 0.00316228 в exclusive (усиление 1); discontinuities 0. Аудио не записывалось. Native probe собран; текущие UI и хост после проверки снова запущены, активный endpoint сохранён.
- Согласно обязательному условию плана снятие компенсации и выпуск переработанного тракта остановлены. На момент этого прогона остальной план не был реализован; последующая независимая работа описана выше. Перезагрузка Windows и чистая установка не проверялись. Подробности: `driver-feasibility.md`, логи/сводка в `results/tag-*`.

## 2026-09-24 — TAG после перезагрузки Windows

- Диагностика: TAG после загрузки отдавал только стандартную capture-линию ID 1; сохранённый endpoint «Mic Noize» (ID 3) был `Unplugged`, а «Microphone (Thin Audio Gateway)» — `Active`. «Обновить устройства» повторяло запуск, но поиск требовал активный endpoint с именем «Mic Noize». Перезапуск хоста сам по себе состояние endpoint не исправил.
- Хост использует активную стандартную линию TAG, если пользовательская линия отсутствует и её старый endpoint отключён. Выбор линии опирается на имя в драйвере, так что переименование endpoint не меняет маршрут. Активный вход на этом ПК переименован через свойства Windows в «Mic Noize (Thin Audio Gateway)»; для Discord его нужно выбрать вручную, если там сохранён старый отключённый вход.
- Проверки: native `mic_tag_host` и `mic_check`, Rust Release собраны и установлены; `mic_check --tag-level-check` прошёл. После переименования и перезапуска хоста живой `--smoke 0 TAG 3 2 40`: 144 480 кадров получено виртуальным микрофоном, `underruns=0`, `drops=0`, `TAG_gaps=0`; UI запущен без повторной ошибки. Не проверено: поведение после следующей перезагрузки и выбор устройства в Discord.

## 2026-09-24 — сворачивание активного окна через панель задач

- Если Windows пропускает событие изменения размера при сворачивании, UI проверяет фактическое состояние окна в фоновом цикле и скрывает его в трей. Кнопка «−» остаётся обычным сворачиванием в панель задач даже при повторном событии.
- Проверки: 16 `controller_tests`, Rust Release build и `git diff --check` прошли. Собранный EXE лежит в `build/rust/release`; запущенный `bin/MicNoize.exe` не заменён. Физический клик по панели задач проверит пользователь.

## 2026-09-24 — прослушивание повтора и последних записей

- Плитки «Последние записи» теперь слышны при включённом «Слышать эффекты» и при «Слышать звуки в наушниках»; обе галки вместе не удваивают звук. Обычный саундпад по-прежнему относится только к галке звуков. Повтор по хоткею уже сохранял категорию эффекта; добавлена проверка его прохождения через выходной микс и очередь прослушивания. Mute и обе выходные ветки TAG/WASAPI используют общий фильтр.
- Проверка: `effects_check` прошёл, включая новые синтетические случаи для горячей клавиши, плитки, совмещённых галок и Mute. Rust Release собран, проектный `bin/MicNoize.exe` обновлён, сверен по SHA-256 и запущен; `mic_tag_host.exe` продолжил работу. Живое прослушивание и физический девайс ещё не проверены.

## 2026-09-24 — громкость плиток «Последние записи»

- Причина: запись Discord сохранялась до выходного коэффициента 0,08, а плитка воспроизводилась с коэффициентом саундпада 0,04 при 100 % и могла дополнительно выравниваться. Новые WAV `(mix)` сохраняют Discord-громкость на момент завершения записи; плитки воспроизводят сохранённый уровень без коэффициента и выравнивания саундпада. Для старых файлов без сведений об источнике применяется прежний коэффициент Discord 100 % (0,08); исходный уровень при иной настройке восстановить нельзя. Mute остаётся финальным.
- Проверки: `effects_check` (сохранение микрофонного/Discord фрагмента, независимость от саундпада, Mute) и 16 `controller_tests` прошли; Rust Release собран, проектный `bin/MicNoize.exe` обновлён и сверен по SHA-256. `cargo fmt --check` не прошёл из-за форматирования многих существующих файлов; массовое переформатирование не выполнялось. Установленная копия из `%LOCALAPPDATA%\MicNoize\current` ещё не обновлена; воспроизведение на слух и физическое устройство не проверялись.

## 2026-09-23 — релиз v0.2.5 опубликован

- Публичный `release.yml` (прогон 35918935240, коммит `46059a3`, старый workflow) прошёл за 26 мин: runtime 13 мин, сборка и `verify.ps1` 12,7 мин. Релиз помечен Latest. `Mic-Noize-Setup-0.2.5.exe` — 24 622 347 байт, SHA-256 `2410ad89045b0fdd369d9e116659722baefeaf8546b75794097273dd3381525f`; `MicNoize-0.2.5-win-x64-stable-full.nupkg` — 20 160 779 байт.
- Не проверено: обновление установленной копии, третий ПК (GTX 1080 Ti) на слух, вход RTX Voice.

## 2026-09-23 — ускорение релизной сборки CI

- Разбор прогона 0.2.4 (27 мин): ~12 мин — скачивание и распаковка `core-runtime-2.part000` (1,15 ГБ) ради одних заголовков, 8,5 мин — холодный `cargo test --release`, 3,7 мин — `cargo build` установщика (это сборка самого exe с thin LTO, а не повтор зависимостей), 1 мин Clippy. Прогон 0.2.5 до правки: шаг runtime снова 13 мин.
- `release.yml`: вместо 1,15 ГБ качается `runtime-headers-2.tar.zst` (34 файла `.h`, 22 376 байт, SHA-256 `a09d6abf68df63ff1d0c907a9ac13123a06dc1ce11d6afc877b730311144e219`, загружен в релиз) из релиза `runtime-core-v2`; сборка C++ — отдельный шаг; кеш Rust (`actions/cache`: registry index/cache, git db, `build/rust`) по точному ключу rustc + `Cargo.lock`, без `restore-keys`, чтобы кеш не рос; перед сборкой удаляются отпечатки `micnoize-*`, так что exe релиза всегда компилируется из текущего коммита. Ревью (8 замечаний) учтено: вытеснение кеша через 7 дней снято постоянным архивом заголовков, рост кеша, устаревший exe, теги.
- Проверки: реплика CI в чистом worktree `.tmp/ci` с одними заголовками — `build.ps1 -NativeOnly` + полный `verify.ps1` (CTest 3/3, `cargo test` 40 passed / 1 ignored, Clippy) прошли за 3,4 мин локально; затем сборка всех пяти нативных целей с нуля только из архива 22 КБ и CTest 3/3. Не проверено: сам новый workflow на GitHub (первый прогон заполнит кеш Rust и будет холодным).

## 2026-09-23 — патч 0.2.5

- Версия 0.2.5 (`release/version.txt`, `ui/Cargo.toml`, `Cargo.lock`), `release/notes.md`. Включает все записи ниже от 2026-09-23 после релиза 0.2.4: исправление DFN, вход RTX Voice/Broadcast, устаревшую ошибку TAG, линию наушников и реверс, автозапуск с Windows, правки UI. Локально перед коммитом: `cargo test --release --locked` 40 passed / 1 ignored, `bridge_check`; полный `verify.ps1` выполняет CI.

## 2026-09-23 — рваный звук DeepFilterNet на GTX 1080 Ti, устаревшая ошибка TAG

- Третий ПК (GTX 1080 Ti, sm 61, CPU-путь DFN): голос «спотыкается», прерывается. Сессионной статистики (`sessions.log`, пишется при остановке) в отчёте не было, поэтому причина установлена по коду: `RuntimeParams::default_with_ch` в libDF жёстко переключает кадры по оценке SNR — ниже −10 дБ кадр целиком обнуляется, выше 20/30 дБ пропускаются стадии декодера (их рекуррентное состояние устаревает). `cpu_denoise.rs` теперь ставит пороги −100/100/100 дБ: каждый кадр идёт через всю модель. Синтетика (гармонический сигнал с огибающей + белый шум, 549 кадров): кадров с RMS выхода < 0,004 при шуме 0,1 — 96 → 65, при 0,2 — 164 → 95; при слабом шуме без разницы. Вычисления не причина: 0,14–0,2 мс на кадр, p99 < 0,8 мс на 7800X3D.
- `dspLoop` на CPU-пути включает FTZ/DAZ (`_mm_setcsr`, до `create`): на 7800X3D p50 0,20 → 0,14 мс; на старых Intel штраф за денормалы больше.
- «Mic Noize TAG microphone endpoint not found» оставалась красной после успешного автоперезапуска (эндпоинт свежеустановленного драйвера появляется с задержкой): во время запуска движок ещё отдаёт старую ошибку, а UI снова её показывал. Показ ошибки состояния 5 теперь пропускается при `busy`, автоперезапуск очищает сообщение.
- GTX 10xx: SDK Audio Effects 3.0 требует Turing+ (модели только turing/ampere/ada/blackwell, `NVAFX_STATUS_GPU_UNSUPPORTED`), так что NVIDIA-шумодав в движке на Pascal невозможен. Но старый RTX Voice на этом ПК работает: вход с именем «RTX Voice» / «NVIDIA Broadcast» теперь считается уже очищенным — `dspLoop` не создаёт ни `Afx`, ни DFN (`stats.denoiser=4`, текст = имя входа, `denoiser=input` в `sessions.log`, C ABI `mnr_denoiser_state` → 4), UI пишет «Шум уже убран на входе», отчёт — «на входе (…)». После правки `bridge_check`, 40 passed / 1 ignored, Clippy чист. Не проверено вживую: на этом ПК нет RTX Voice/Broadcast.
- Проверки: `bridge_check` прошёл; `cargo test --release --locked` 40 passed / 1 ignored, включая новый `every_frame_runs_the_full_model` и прежние `digital_silence_does_not_replay_old_audio`, `failed_session_restarts_with_backoff_and_gives_up`. Clippy `--all-targets -D warnings` чист. Не проверено: на слух и на самом третьем ПК, живой тест `engine_runs_deepfilternet_live`.

## 2026-09-23 — линия наушников, автозапуск, вкладка «Наушники»

- Причина «Headphone host rejected request»: демо-драйвер TAG держит три линии, и их занимали стандартные «TAG Microphone» / «TAG Speakers» плюс «MicNoize Microphone»; `CreateLine` для «MicNoize Headphones» → `0x80070044`. `TagOutput` теперь удаляет стандартные линии «TAG …» перед поиском/созданием своей и перечитывает список; при отказе создания текст ошибки перечисляет линии драйвера. `mic_tag_host` пересобран и перезапущен (UI поднял его сам). Не проверено: успешный запуск наушников после правки, судьба стандартных линий после перезагрузки/переустановки драйвера (при старте хоста удаляются снова).
- Автозапуск приложения с Windows по умолчанию: `[ui] app_autostart=1`, HKCU Run `MicNoize` → `"<exe>" --start-tray`, галка в настройках (фокус 79). Реальный вход в Windows не проверялся; из терминала агента запись HKCU виртуализирована MSIX, поэтому проверять запуском из Проводника.
- «Наушники»: убрана кнопка «Без звука» (Mute больше не выставляется, в C ABI передаётся `false`); галка NVIDIA переключается при работающих наушниках (стоп + старт в новом режиме), она же — заголовок ползунка силы; «Включить наушники» — кнопка на всю ширину колонки. Контроллер-тест `headphones_are_off_and_independent` обновлён. Сборка, 39 passed / 1 ignored, Clippy чист, снимок `--ui-headphones` осмотрен.
- «Наушники» → галка **Реверс** (`[headphones] reverse`, C ABI `mnr_headphone_reverse`): `GrainReverse` в `effects.hpp` — окна 200 мс с шагом 100 мс, каждое задом наперёд под периодическим окном Ханна (сумма 1), +200 мс только пока включено, переключение и новая эпоха начинают с пустых буферов. `effects_check` (единичное усиление на постоянном сигнале, границы, пила разворачивается, сброс в тишину) и `bridge_check` прошли. На слух не проверялось.

## 2026-09-23 — правки UI по замеру заметности

- Основание: скриншоты всех вкладок работающей 0.2.4 прогнаны через модель заметности Itti–Koch (цвет/яркость/ориентация) с перекрёстной проверкой OpenCV FineGrained и SpectralResidual; медиана по 81 варианту (сдвиг/обрезка кадра × радиус торможения возврата). Скрипты и рисунки — `.tmp/saliency` (не в Git). Отмеченные оранжевые галочки («Перегрузка», «Слышать результат эффектов») брали фиксацию №3 раньше шумоподавления; серые — №8–9, шумодав №7 → №6. Эффект виден только цветовой модели (оранжевый и DIM близки по яркости). Сработавший эффект и так получает фиксацию №1 (горящий хоткей), поэтому заливки ползунков не приглушались; серые шевроны маршрута и жирный заголовок шумодава эффекта не дали и не внесены.
- `check_style`: отмеченная галочка заливается DIM (наведение — INK) на всех вкладках, включая автозапуск в настройках (раньше принудительно оранжевый). `action()` учитывает `Disabled`: обычная кнопка теряет заливку, акцентная бледнеет до 35 %. «Обновить сейчас» рисуется только при готовом обновлении (порядок Tab уже его пропускал). «Установить RVC runtime» — акцентная; «Импорт модели» без runtime — DIM.
- «Микрофон»: строка 101–200 % показывается только выше 100 % (оранжевым); громкость Discord перенесена над «Повтором последнего», чтобы повтор стоял над своими записями; пустая строка статуса прослушивания больше не занимает место; подписи «Слышать эффекты» / «Слышать усиление»; подпись кнопки прослушивания DIM, когда она недоступна.
- Ползунки: `marked_slider` (стек Iced под ползунком, геометрия как у бегунка) — серая метка 100 % на шумодаве (оба уровня и наушники), громкости Discord, общей громкости саундпада, входе RVC; знаковые (высота микрофона и наушников, тон RVC) заливаются от нуля, при 0 без заливки. «Замедление» зеркально (`-95..=-50`), стрелки клавиатуры развёрнуты. Ctrl+клик (`Slider::default`) сбрасывает к исходным значениям загрузчика настроек. «Наушники»: заголовок силы как у соседних, без галки NVIDIA ползунок и заголовок бледнеют, предупреждение только выше 100 %.
- Voice Changer приведён к виду остальных вкладок: без карточки-панели, заголовки «Тон модели» / «Влияние индекса» / «Вход модели» — жирное имя слева, значение справа, ползунок на всю ширину, разделители между группами (общий `heading`). После этой правки собрано, тесты и Clippy прошли; снимок `--ui-rvc` проверен. «Наушники»: параметры (сила, громкость, высота, пояснения) собраны в центрированную колонку 600 px, маршрут и кнопки остаются на всю ширину; снимок `--ui-headphones` проверен, UI переустановлен в `bin` и перезапущен.
- Проверки: `cargo build --release --locked` без предупреждений, `cargo test --release --locked` 39 passed / 1 ignored (в клавиатурный тест добавлена проверка зеркального «Замедления»), Clippy `--all-targets -D warnings` чист. Не проверено: снимок собранного UI (`--ui-snapshot`) и живой вид при 100–200 % масштабе, реальное поведение Ctrl+клика мышью; установленная копия не обновлялась. Замер заметности — модель восходящего внимания на статичном кадре, не айтрекинг: движение индикатора и смысл текста не учитываются.

## 2026-09-23 — модели NVIDIA по архитектуре GPU, переоткрытие прослушивания, вкладка «Логи»

- Второй ПК: `Load model: NVIDIA status 1` и бесконечный автоперезапуск. Причина (наиболее вероятная, GPU второго ПК не подтверждён): движок всегда грузил `models/ampere`, а TensorRT-модели собраны под одну архитектуру; на RTX 20xx/40xx/50xx `NvAFX_Load` падает. Теперь `gpuArch` (C++, `nvcuda.dll`, CUDA device 0) выбирает `turing|ampere|ada|blackwell`, текст ошибки называет GPU и архитектуру, ошибки SDK пишутся в `results/nvafx.log`. C ABI `mnr_gpu`; UI при отсутствии моделей своей архитектуры докачивает компонент `runtime-models-v3.0.0/models-<arch>.json` в рамках установки core. `package-components.ps1` получил виды `models-<arch>`. **Модели turing/ada/blackwell ещё не скачаны с NGC и релиз `runtime-models-v3.0.0` не опубликован** — до этого второй ПК получит ошибку загрузки компонента и понятную ошибку движка вместо `status 1`.
- Второй ПК: `TAG driver not found (run install-tag.ps1): HRESULT 0x80070490` пишет старый `mic_tag_host.exe` из core v2 (собран 2026-09-21, до смены текста); `0x80070490` = драйвер не найден в момент запуска хоста. Первопричина без логов второго ПК не установлена; хост не пересобирался.
- Этот ПК: `Прослушивание: Monitor padding: HRESULT 0x88890004` = `AUDCLNT_E_DEVICE_INVALIDATED`. Прослушивание теперь переоткрывает поток на текущем выходе Windows (0,5 с, до 20 попыток подряд) вместо остановки с ошибкой; `check()` бросает `DeviceLost` для этого кода.
- Вкладка «Логи» (страница 5, фокус 76/77/78/73): один отчёт (версия, GPU, драйвер NVIDIA, Windows, состояние, хвосты логов со временем изменения, повторы схлопнуты, `%USERPROFILE%` замаскирован), «Копировать всё», «Открыть папку», «Отправить разработчику» (перенесена из настроек). Новый `results/app.log`: запуск с GPU и каждое новое сообщение статуса с UTC-временем. QA-флаг `--ui-logs`.
- Проверки: нативные `mic_engine`, `bridge_check`, `effects_check`, `mic_check` собраны; `bridge_check`, `effects_check`, `mic_check --self-test` прошли; `cargo test --release --locked` 35/35 (новые `logs_page_is_reachable_by_keyboard`, `logs::tests::tail_keeps_newest_lines_and_collapses_repeats`); Clippy `--all-targets -D warnings` чист; разовый вызов `engine::gpu()` на этом ПК вернул `ampere, NVIDIA GeForce RTX 3090`. Снимки `--ui-logs` при 100 % и 200 % (`results/ui-logs.png`): моноширинный fallback не имел кириллицы и обрезал `_`, текст переведён на Consolas. GPU второго ПК подтверждён: RTX 5070 (Blackwell). Не проверено: реальная загрузка модели после изменения пути, переоткрытие прослушивания при переподключении наушников, второй ПК.

## 2026-09-23 — релиз v0.2.4 опубликован

- Публичный `release.yml` (прогон 35877959739, коммит `8be8b93`) завершился успешно примерно за 27 мин: `verify.ps1` в CI прошёл (с git-зависимостью DeepFilterNet), релиз опубликован и помечен Latest. Установщик `Mic-Noize-Setup-0.2.4.exe` — 24 607 662 байта (0.2.3: 11 620 393), SHA-256 `53f5a6759e377707cc450c9c24d4cafe1dedfc801970c5fb75b9ac4ecc8ad564`; полный `MicNoize-0.2.4-win-x64-stable-full.nupkg` — 20 146 094 байта. Лента `releases.win-x64-stable.json` содержит 0.2.4 (Full).
- Обновление установленной копии с 0.2.3 и второй ПК (RTX 5070) после выхода не проверялись.

## 2026-09-23 — патч 0.2.4

- Финальное ревью нашло в libDF: кадр со средним квадратом < 1e-7 возвращает нули, не продвигая буферы, и после цифровой тишины (аппаратный mute HyperX QuadCast) первые 30 мс повторяли звук до неё (стенд: 0,13–0,14 RMS против 0,0055). Обёртка подмешивает к тихим кадрам тон 24 кГц −64 dBFS, чтобы модель шла дальше, и после 4 тихих кадров подряд отдаёт точные нули. Тест `digital_silence_does_not_replay_old_audio`. `DfTract::init` не полный сброс, поэтому не используется.
- Строка статуса при загрузке теперь «Загружаем компоненты NVIDIA/TAG…» (включает модели).
- Опубликован `runtime-models-v3.0.0` (не Latest) с `models-turing|ada|blackwell`. Установка через `install()` приложения из опубликованных URL во временную папку: подписи, SHA-256 архивов и SHA-256 всех 6 моделей против `nvidia-assets.sha256` сошлись, `version.txt` = 3.0.0.
- Версия 0.2.4 (`release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock`, `release/notes.md`). `verify.ps1` прошёл: CTest 3/3, Rust 39/39 (1 ignored live), Clippy `-D warnings`. Живой DFN-сеанс повторён после фикса: `process_ms=0.201`, 0 обрывов.
- Не проверено: RTX 5070 на втором ПК, GTX/AMD, качество DeepFilterNet на слух, переподключение наушников при прослушивании.

## 2026-09-23 — DeepFilterNet 3 на процессоре

- Для ПК без подходящей RTX (GTX 10xx/16xx, AMD, Intel, нет модели) шумодав теперь DeepFilterNet 3: `ui/src/cpu_denoise.rs` на `deep_filter` v0.5.6 (git, коммит `978576aa`, `tract` + встроенная модель; на crates.io только 0.2.5 без инференса). Движок получает его указателями функций (`mnr_set_cpu_denoiser`, `mic::CpuDenoiserApi`), поэтому `bridge_check`/`effects_check`/`mic_check` линкуются без Rust. `stats.denoiser=3`, `sessions.log` `denoiser=cpu`, UI показывает режим под регуляторами. Сила слайдера → предел ослабления (≥100 % — полное, иначе 40 дБ × сила, минимум 0,5 дБ: при ровно 0 дБ libDF отдаёт вход без своей задержки). `MNR_DENOISER=cpu` включает CPU и на RTX.
- Замеры на Ryzen 7 7800X3D (стенд `.tmp/dfn-probe`, не в Git): инициализация 213 мс, кадр 480 сэмплов avg 0,315 / p99 0,706 / max 1,03 мс; задержка по корреляции 1440 сэмплов = 30,0 мс. Живой сеанс `engine_runs_deepfilternet_live` (ignored-тест, HyperX QuadCast → DELL S2421H, Mute): `state=3`, `denoiser=3`, `process_ms=0.212`, 0 обрывов, 0 сбросов за 8 с. `MicNoize.exe` 11,2 → 34,4 МБ.
- Проверки: `bridge_check`, `effects_check`, `mic_check --self-test` прошли; Rust 38/38 (+ `strength_maps_to_a_delay_stable_attenuation`, `deepfilternet_runs_480_sample_frames`), 1 ignored live; Clippy `-D warnings` чист; `--locked` сборка с обновлённым `Cargo.lock`. Не проверено: качество на слух, GTX/AMD вживую, наушники (по-прежнему только NVIDIA), сборка в CI (`release.yml` теперь тянет git-зависимость).

## 2026-09-23 — движок без NVIDIA, ревью

- Ревью диффа: (1) логгер SDK включался `call_once`, а Stop выгружает `NVAudioEffects.dll` — со второго сеанса лог не писался; теперь он включается при каждой свежей загрузке DLL (`GetModuleHandleW` до `LoadLibraryExW`). (2) Ошибка загрузки моделей блокировала установку драйвера TAG на чистом ПК; теперь драйвер ставится, если core на диске, а результат пишется в `app.log`. (3) «Отправить разработчику» теперь шлёт и `app.log`, `nvafx.log`.
- Если `Afx` не создаётся (не RTX, нет модели, ошибка загрузки), DSP-поток работает с `out = in` вместо сбоя сессии и бесконечного автоперезапуска; причина в `mnr_denoiser_state`, UI показывает её под регуляторами шумоподавления, `sessions.log` получил `denoiser=nvidia|bypass|none`. Наушники не менялись.
- Проверки: `mic_engine`, `bridge_check`, `effects_check`, `mic_check` собраны; `bridge_check`, `effects_check`, `mic_check --self-test` прошли; Rust 36/36, Clippy `-D warnings` чист, release собран. Живой сеанс в режиме без шумодава не прогонялся (TAG занят установленным 0.2.3; естественная проверка — второй ПК, пока модели Blackwell не опубликованы).

## 2026-09-23 — версия моделей и проверка упаковки

- `models_installed` требует оба файла и `version.txt` = `MODELS_VERSION` (3.0.0; папка без файла считается 3.0.0 из core v2). Смена константы вместе с `MODELS_RELEASE` заставит все установки перекачать модели. `package-components.ps1` пишет `version.txt` в пакет. Тест `models_need_both_files_and_the_current_version`; Rust 36/36, Clippy чист.
- Сквозная упаковка `models-ampere` (как проверка конвейера, не публиковалась): архив 153 197 873 байт, `version.txt` внутри, раскладка путей верна, подпись манифеста проходит `verify()` приложения.
- NGC CLI и `download_features.ps1` получают токен, но API отвечает `Org contex missing` (ключ привязан к личной org). Модели turing/ada/blackwell 48 кГц 3.0.0 скачаны через File Browser каталога NGC в залогиненном браузере пользователя; все 6 `.trtpkg` сошлись с `.md5` NVIDIA, SHA-256 добавлены в `nvidia-assets.sha256`, файлы лежат в `vendor/.../models/<arch>`.
- Собраны пакеты `models-turing` (168 100 774 байт), `models-ada` (154 083 285), `models-blackwell` (154 762 432) в `.tmp/component-models-<arch>/out`: подписи всех трёх манифестов проходят `verify()` приложения, URL ведут на `runtime-models-v3.0.0`, в архивах обе модели и `version.txt`. Релиз `runtime-models-v3.0.0` ещё не опубликован; загрузка на RTX 5070 не проверена.

## 2026-09-23 — релиз v0.2.3 опубликован

- Публичный `release.yml` (прогон 35807319337, коммит `35d8048`) завершился успешно за ~20 мин: `verify.ps1` в CI прошёл, релиз опубликован и помечен Latest. Установщик `Mic-Noize-Setup-0.2.3.exe` — 11 620 393 байта, SHA-256 `43d110f9bd56f9097d70eeea4a6551c4226527a005bb286f664cc28bce14df96`; полный `MicNoize-0.2.3-win-x64-stable-full.nupkg` — 7 158 825 байт. Лента `releases.win-x64-stable.json` содержит 0.2.3 (Full).
- Обновление установленной копии с 0.2.2 на 0.2.3 на этом ПК не прогонялось; прослушивание саундпада после выравнивания и игры не проверены.

## 2026-09-23 — патч 0.2.3

- В релиз входят автоперезапуск после сбоя, выравнивание громкости саундпада, новая шкала громкости саундпада, клик по панели задач → трей, новые поля `sessions.log` и повторная проверка перед обновлением.
- Обновление: проверка шла только при старте и по кнопке, а «Обновить» ставило уже скачанный пакет; в программе, неделями живущей в трее, это давало два обновления подряд (сначала устаревший скачанный, потом свежий). Теперь `Msg::ApplyUpdate` сначала заново вызывает `check_and_download` и применяет результат; при `Unavailable` (нет сети) ставится скачанный пакет, при `Current` (релиз отозван) ничего. Тесты `ready_update_is_reachable_from_any_page` (расширен) и `offline_recheck_still_applies_the_downloaded_update`; `verify.ps1` прошёл (Rust 33/33). Версии в `release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock` и заметках релиза обновлены до 0.2.3.
- Живой тест автоперезапуска (с разрешения пользователя): `mic_tag_host` убит по PID в 00:34:30Z, сбой зафиксирован в 00:34:31Z (`failed_at`), через 2 с UI перезапустил обработку, новый хост поднят, движок снова владеет TAG примерно через 9 с после убийства (около 5 с — загрузка модели NVIDIA). Закрытая сессия длилась ~33 мин: `run_max_ms=3.41`, `runs_over_5ms=0`. `verify.ps1` прошёл (CTest 3/3, Rust 32/32, Clippy `-D warnings`).
- Не проверено: выравнивание саундпада на слух, клик по панели задач вручную, игры. Сбой хоста обнаруживается только при записи в него, то есть пока Discord или другой клиент захватывает виртуальный микрофон; без клиента мёртвый хост заметят при следующем захвате.

## 2026-09-23 — автоперезапуск после сбоя и разбор пиков NVIDIA

- Сбой обработки раньше был окончательным: движок переходил в `state=5`, кнопки «Старт» нет, и голос не обрабатывался до «Обновить устройства». Разбор `TAG host stopped responding`: строка в `sessions.log` пишется только при следующем `stop()` (в 22:20:36Z это был новый старт, который и поднял нынешний хост: командная строка `bin/mic_tag_host.exe` из `tagHostProcess`), поэтому время сбоя и длительность простоя не восстановить. Старый хост исчез без записи в `tag-host.log` и без событий WER; кто его завершил, неизвестно, Arkanoid Startuper Watcher хост не трогает.
- UI теперь сам перезапускает обработку после любого сбоя через 2/5/15/30/60 с с обратным отсчётом в сообщении, затем сдаётся; минута нормальной работы после последнего сбоя обнуляет попытки, ручной старт снимает ожидание. Включается только после первого автостарта и не во время установки компонентов. Тест `failed_session_restarts_with_backoff_and_gives_up`.
- `sessions.log`: добавлены `runs_over_5ms`, `runs_over_10ms`, `run_max_at_s` и `failed_at` (UTC первого сбоя). `mic_check --bench-afx` получил режим `churn`.
- Пики `run_max_ms` 9,9–12,2 мс не воспроизведены: офлайн 60 с на блок NVIDIA дают максимум 2,0 мс (авто CUDA graphs), при 16 загруженных потоках ЦП 2,0 мс, при 100 % загрузке GPU вычислениями 3,1 мс, после `reset` 0,9 мс, после смены силы 0,01 мс (`results/afx-spikes-2026-09-23.txt`). Модель укладывается с запасом; пики — редкие внешние события (кандидаты: вытеснение графикой в игре, смена режима питания GPU), буфер 40 мс их поглощает (0 обрывов во всех сеансах). Новые поля покажут частоту.
- Проверки: `mic_check --self-test`, `bridge_check`, `effects_check` прошли; `cargo test --release --locked` 32/32; Clippy без предупреждений; dev UI пересобран и перезапущен, TAG host не трогался, движок снова владеет TAG. Не проверено: живой сбой хоста с автоперезапуском (требует убить хост), игры, долгие сеансы с новыми полями.

## 2026-09-23 — выравнивание громкости саундпада

- Галка «Выравнивать громкость» (по умолчанию включена, `[soundpad] normalize`): после декода клип измеряется по BS.1770 (K-фильтр, блоки 400 мс, гейты −70 LUFS и −10 LU, расчёт в f64) и масштабируется в окно −18…−12 LUFS (`norm_min`/`norm_max`). Подъём ограничен +12 дБ и пиком −1 dBFS, приглушение не ограничено. Файлы, C++ и C ABI не менялись; громкость строки и общая громкость работают поверх. Касается и записей эффектов. Галка в Tab-порядке после общей громкости (фокус 75).
- Закрыта гонка перезагрузки: движок хранит клипы по id, и декод, начатый до `sound_clear`, мог после «Обновить» или переключения записать в движок устаревший или чужой клип. Теперь задача сверяет поколение списка (атомарное зеркало `sound_generation`/`clip_loads`) прямо перед загрузкой; `rescan_sounds` также меняет поколение записей. Остаётся микросекундное окно между проверкой и загрузкой.
- `cargo test --release --locked` прошёл (31/31, новый `soundpad::tests::normalize_window`: эталон −3,01 LUFS для синуса 997 Гц, приглушение, окно без изменений, предел +12 дБ, потолок пика, тишина, короткий клип, разбор настроек; расширен `soundpad_keys_volumes_and_keyboard`), `cargo clippy --all-targets` без предупреждений, `cargo build --release --locked` прошёл. Dev UI заменён и перезапущен через Explorer, TAG host не трогался. Прослушивание в наушниках и Discord, скорость на 300-секундном клипе и масштаб 100–200 % вручную не проверялись.

## 2026-09-22 — клик по панели задач убирает окно в трей

- Системное сворачивание окна (клик по значку в панели задач у активного окна, Win+M) теперь скрывает его в трей, как в OBS. Кнопка «−» по-прежнему сворачивает в панель задач. Распознаётся по событию `Resized(0×0)`, которое winit шлёт при `WM_SIZE`/минимизации; при недоступном трее окно остаётся свёрнутым.
- `cargo test --release --locked ... controller_tests` прошёл (13/13, в тесте фонового окна добавлены оба сценария), `cargo build --release --locked` прошёл; dev UI в `bin` заменён и перезапущен через Explorer, TAG host не трогался. Клик по панели задач вручную не проверялся.

## 2026-09-22 — новая шкала громкости саундпада

- Прежние 20 % общей громкости стали новыми 100 %: физический gain 0,04. Сохранённое значение пересчитывается при загрузке (старые значения выше 40 % ограничиваются новым максимумом 200 %); отдельная громкость клипов не меняется. Новое точное значение хранится в `volume_display`, а старый `volume` остаётся на прежней шкале для безопасного отката.
- `cargo test --release --locked ... controller_tests::soundpad` прошёл (2/2), `cargo build --release --locked` прошёл. Dev UI заменён собранным файлом и запущен в трей; TAG host не перезапускался. В активной настройке `volume=20` автоматически дополнено `volume_display=100`, без смены старого значения. `cargo fmt --check` не прошёл из-за многочисленных расхождений форматирования в Rust-проекте; автоматическое форматирование не запускалось. Прослушивание человеком не проводилось; релиз этой правки не опубликован.

## 2026-09-22 — релиз v0.2.2 опубликован

- Публичный `release.yml` (прогон 35777115637, коммит `b17af73`) завершился успешно: сборка и проверки прошли, релиз опубликован. Установщик `Mic-Noize-Setup-0.2.2.exe` — 11 617 934 байта, SHA-256 `7ea80fdef7df43bc1590ba2cafc9da083321edc0485b3f56aadb612c4dd7f948`; также опубликованы полный `.nupkg` и файлы канала `win-x64-stable`.
- В релиз входят исправление разделов саундпада и компенсация уровня TAG. Локальные проверки перед публикацией повторно не запускались; реальное прослушивание, эксклюзивный захват и работа на других ПК не проверены.

## 2026-09-22 — патч 0.2.2

- В релиз входят исправление пустых разделов саундпада и компенсация уровня виртуального микрофона TAG. Версии в `release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock` и заметках релиза обновлены до 0.2.2.

## 2026-09-22 — нейтральный уровень TAG без смены драйвера

- При работающем TAG Mic Noize удерживает Windows endpoint на максимуме и ослабляет PCM на измеренное максимальное усиление в дБ; другие пути вывода не меняются. Отдельный поток проверяет уровень и системный Mute каждые 50 мс, не задерживая аудиопоток. На текущем ПК диапазон −96…+30 дБ, коэффициент 0,0316228. Компенсация рассчитана на общий режим захвата: в эксклюзивном Windows обходит программную громкость endpoint и звук будет тише.
- `mic_check --self-test`, `--tag-level-check` и `--tag-level-watch-check` прошли. Последний проверил возврат уровня и Mute после внешнего изменения; dev UI собран и перезапущен, TAG host оставлен работающим. Слуховое сравнение и другие ПК не проверялись.

## 2026-09-22 — содержимое разделов саундпада

- После перехода из длинного списка в короткий раздел или сужения поиска старое смещение прокрутки могло оставить список без отрисованных строк. Расчёт видимых строк теперь ограничивает смещение длиной текущего списка.
- Точечный Rust-тест `sound_rows_stay_bounded_with_distant_focus` прошёл (1/1). В работающем приложении визуально не проверялось; релиз не публиковался.

## 2026-09-22 — релиз v0.2.1 опубликован

- `release.yml` на публичном репозитории (`workflow_dispatch`, версия 0.2.1, прогон 35766424821) прошёл успешно: релиз `v0.2.1` содержит `Mic-Noize-Setup-0.2.1.exe` (11 611 115 байт, SHA-256 `101E0F2C968F1787B17B2987EFE66773905611C6D7F1824A90D5BEA80E0758B3`, те же байты в `Setup.exe` и `MicNoize-win-x64-stable-Setup.exe`), полный `.nupkg` и фид `RELEASES-win-x64-stable`. Дельты нет: воркспейс CI пуст, клиенты 0.2.0 скачают полный пакет 7,1 МБ.
- Коммиты `85dac5a` + `759ec74` (и следом документация `eff9f7a`) в `origin`; в публичный репозиторий перенесены черри-пиком как `2820075`, `ffd540c`, `05801d9`. `git push release main` не использовался.
- Важно про заметность обновления: панель приходит **вместе с** 0.2.1, поэтому переход 0.2.0 → 0.2.1 пользователь по-прежнему увидит только в «Настройках». Панель начнёт работать как уведомление со следующего релиза.
- Не проверено: реальная установка `Setup.exe` 0.2.1 на чистом ПК, запрос UAC и появление виртуального микрофона; цикл обновления через Velopack с новой панелью.

## 2026-09-22 — версия 0.2.1

- Публичный релиз v0.2.0 вышел 2026-09-21 и содержит ту самую поломку с драйвером, поэтому сегодняшние исправления выходят как **0.2.1**: `release/version.txt`, `ui/Cargo.toml`, `ui/Cargo.lock` и `release/notes.md` обновлены.
- `verify.ps1` на 0.2.1 прошёл целиком (нативные проверки, CTest, Rust 29/29, строгий Clippy), exit 0.

## 2026-09-22 — готовое обновление видно на всех вкладках

- Скачанное обновление было заметно только тому, кто сам зашёл в «Настройки». Теперь под строкой вкладок на любой странице появляется панель «Доступно обновление» со статусом (`Версия X скачана и готова`) и оранжевой кнопкой «Обновить и перезапустить» — та же `Msg::ApplyUpdate`, что и в настройках. Панель существует только пока обновление скачано и готово, закрывать её нечем: она исчезает после установки.
- Панель входит в Tab-порядок каждой страницы первым элементом, Enter/Space на ней применяет обновление. Заодно в Tab-порядок настроек добавлены пропущенные кнопки «Установить виртуальный микрофон» (когда драйвера нет) и «Отправить логи разработчику», обе активируются с клавиатуры.
- Проверки: новый controller-тест `ready_update_is_reachable_from_any_page` (без обновления баннера в порядке нет, с обновлением он достижим на главной и в настройках, Enter взводит применение), Cargo test 29/29, `verify.ps1` целиком (нативные проверки, CTest, Rust, строгий Clippy) — exit 0. Снимок `results/ui-update-banner.png` снят с временной заглушкой готового обновления, заглушка откачена.
- `bin/MicNoize.exe` обновлён (SHA-256 `3EF950135881E26DAA1831606BA3FF3AA53E292C88FBE87C8D7C59B9EB4126BE`), UI остановлен и не перезапускался, TAG host не трогали. Не проверялось: реальный цикл обновления через Velopack с этой панелью (нужен опубликованный релиз новее установленного).

## 2026-09-22 — своя телеметрия Mic Noize и причина дневных лимитов Cloudflare

- Лимиты на аккаунте выбирал не объём телеметрии, а рейт-лимитер воркера: `telemetry/worker/src/ratelimit.ts` в Moment Player делал `kv.put` на каждый запрос ingest/party/room, а бесплатный план даёт 1000 записей KV в сутки **на аккаунт**. Счётчики перенесены в изоляты воркера (окно 60 с, очистка после 10k ключей, тестовый сброс `resetRateLimits()`); записей в KV больше нет. Холодный старт сбрасывает окно — это защита от спама на уровне установки, а не глобальный лимит. Там же: `npm test` 91/91, `tsc --noEmit` чисто.
- Mic Noize получает отдельный воркер `micnoize-telemetry` со своей базой D1 и своим `/admin`, из того же исходника через `telemetry/worker/wrangler.micnoize.toml`. `ENDPOINT` в `ui/src/telemetry.rs` переключён на него. Квоты Cloudflare считаются по аккаунту, поэтому разделение даёт изоляцию данных и деплоев, а не дополнительные лимиты.
- В панели оператора приложения разделены не вкладками, а списком «Filter by application» (`all` / Moment Player / Mic Noize / legacy), и он показывается только на вкладках со здоровьем и инсталлами.
- Воркер задеплоен: база D1 `micnoize-telemetry` (c41e93a9…) создана, 13 миграций применены, `https://micnoize-telemetry.arkanoidvfx.workers.dev` отвечает, `/admin` отдаёт 302 на логин. Деплой без привязки Durable Object `ROOM` прошёл нормально: неиспользуемый экспорт класса wrangler не смущает. Оба воркера (Moment Player и Mic Noize) задеплоены с одного исходника.
- Найдена и закрыта дыра: пока секрет не выставлен, ingest принимал `X-Hmac: dev-mode` **в проде** — свежезадеплоенный воркер был открытым эндпоинтом (проверено живым запросом: 202). Теперь обход возможен только при `ENVIRONMENT` dev/test или запросе на localhost, а без секрета ответ 503 `ingest_not_configured` вместо 500 из WebCrypto. На Moment Player поведение не изменилось: секреты там выставлены, неподписанный запрос получает 401. Строка, записанная проверочным запросом, удалена — база `micnoize-telemetry` пуста (installs/sessions/events = 0).
- Осталось вручную: выставить четыре секрета нового воркера (`MICNOIZE_HMAC_SECRET` = `MNR_TELEMETRY_SECRET` из GitHub, `ADMIN_PASSWORD`, `ADMIN_TOKEN`, `ADMIN_SESSION_SECRET`) — `wrangler secret put` спрашивает значение интерактивно. До этого телеметрия и отчёты из релизной сборки будут получать 503.

## 2026-09-22 — любой микрофон по умолчанию, повтор установки драйвера и отправка логов

- Автовыбор микрофона работал только для устройств со словом `HyperX` в названии: на чужом ПК ничего не выбиралось и автозапуск молчал. `devices()` в `audio.cpp` теперь ставит первым микрофон связи по умолчанию Windows (`GetDefaultAudioEndpoint(eCapture,eCommunications)`), а контроллер без сохранённого устройства берёт HyperX, иначе первый в списке. Сохранённое устройство по-прежнему либо совпадает точно, либо остаётся невыбранным.
- В настройках, пока виртуального микрофона нет, показывается подпись и кнопка «Установить виртуальный микрофон»: отказ от UAC больше не требует перезапуска программы. Кнопка и подпись исчезают после успешной установки (`driver_ready`).
- Кнопка «Отправить логи разработчику» рядом с обновлениями: `telemetry::report` шлёт одним запросом заметку о текущем состоянии UI и хвосты (по 2 КБ) `results/tag-host.log`, `sessions.log`, `tag-headphones.log` как события `error` (`source: micnoize`). Они попадают в ту же панель оператора, что и ошибки Moment Player, и фильтруются по `app_id = mic_noize`; схема воркера не менялась. `send()` теперь отправляет пакет событий, а не одно.
- Проверки: release-сборка, строгий Clippy, Cargo test 28/28, `bin/bridge_check.exe`.
- Не проверялось: реальная доставка отчёта (локальная сборка собирается без `MNR_TELEMETRY_SECRET`, поэтому отправка в ней отключена по замыслу — проверять нужно на релизной сборке), внешний вид новых кнопок в живом окне (снимок UI не снимался, чтобы не останавливать работающий UI и TAG-хост), выбор микрофона на ПК без HyperX.

## 2026-09-22 — программа сама ставит TAG-драйвер на чистом ПК

- По отчёту с другого ПК: после установки `Setup.exe` UI показывал «Cannot start mic_tag_host.exe; run build.ps1», а затем `results/tag-host.log` дважды писал «TAG driver not found (run install-tag.ps1): HRESULT 0x80070490». Причина не в сборке: в поставку входят только `MicNoize.exe`, лицензия и заметки, а установить подписанный TAG-драйвер было нечем — `install-tag.ps1` не входит в пакет и требует `signtool.exe` из Windows SDK. У любого нового пользователя виртуальный микрофон не появлялся вообще.
- `components.rs`: `driver_installed()` читает узел устройства `HKLM\SYSTEM\CurrentControlSet\Enum\Root\ThinAudioGateway_4d699d4a 0` (без прав администратора), `install_driver()` запускает `wdmdrvmgr.exe -q -h <instance> -i <hardware id> <inf>` из скачанного core-компонента через `Start-Process -Verb RunAs` (полный путь к `powershell.exe` из `%SystemRoot%`). Тот же INF и те же идентификаторы, что у `install-tag.ps1`; настройки безопасности Windows не меняются, проверка подписей через SDK остаётся путём разработчика.
- Контроллер: драйвер ставится при старте, если core-компонент уже на месте, иначе сразу после его установки; на время установки автозапуск обработки ждёт, в шапке видно «Устанавливаем виртуальный микрофон: подтвердите запрос Windows…». Отказ от UAC показывает просьбу перезапустить и подтвердить — повторный запрос без рестарта не делается.
- `bridge.cpp` больше не зовёт `ensureTagHost()`, пока `bin/mic_tag_host.exe` не скачан (`tagHostInstalled()` в `tag_link.hpp`): гонка со стартом UI давала пользователю dev-текст про `build.ps1`. Обе оставшиеся ошибки TAG переведены на русский («Виртуальный аудиодрайвер не установлен», «Не удалось запустить фоновый процесс виртуального микрофона»).
- Проверки: Cargo test 28/28 с новым `driver_install_script_quotes_paths_with_spaces`, строгий Clippy, release-сборка UI, `bin/bridge_check.exe`. Кавычки аргументов проверены отдельно подставным процессом: Windows PowerShell не экранирует элементы `-ArgumentList` с пробелами, поэтому путь INF несёт собственные кавычки и доходит до программы одним аргументом (`%APPDATA%\Mic Noize\Components` содержит пробел).
- Не проверялось: реальная установка драйвера на чистом ПК и сам запрос UAC (на этой машине драйвер уже установлен, переустановка оборвала бы работающий TAG-хост), поведение при отказе от UAC, обновление `bin/MicNoize.exe` и выпуск нового `Setup.exe`. Вопрос о правах на распространение TAG demo эта правка не решает.

## 2026-09-22 — усиление не пишется в повтор; список последних шести записей

- По отчёту пользователя удержание усиления заменяло фрагмент «Повтора последнего». `LastEffect` теперь маскирует оба бита усиления (`HoldBoost`, `HoldBoost<<DiscordShift`) и не берёт сэмплы, у которых нет категории, кроме `ModifiedBoost`; сохранённая категория тоже очищается от него. Фрагменты создают только высота, замедление, ускорение и реверс.
- Законченный фрагмент публикуется движком (`Engine::clipCopy`, C ABI `mnr_last_clip`) без аллокаций в DSP: слот резервируется при старте, публикация идёт по `try_lock` и повторяется на следующем блоке при конкуренции. UI забирает его по генерации и пишет WAV 48 кГц моно 16 бит в `%APPDATA%\Mic Noize\Записи`; хранятся шесть новейших файлов, остальные удаляются.
- На вкладке «Микрофон» под громкостью Discord появился блок «Последние записи»: воспроизведение через тот же `SoundPlayer` (id от 900000, вне диапазона клипов саундпада), поведение стоп/перезапуск — как в саундпаде, плюс кнопка «↓» с выбором «В папку саундпада» (при пустой папке сначала её выбор, затем копия и пересканирование библиотеки) или «В другую папку». Имя-дубликат получает ` (2)`. Плитки и обе кнопки меню включены в Tab-порядок вкладки.
- Чтобы шесть записей помещались без прокрутки в окне 820×820: плитки по три в ряд (две строки по 22 px вместо шести строк по 28 px), подпись — только время, длительность и дата убраны с плитки, заголовок блока совмещён с результатом сохранения и с меню выбора папки, обе галочки монитора перенесены в левый столбец под «Слышать весь голос». Tab-порядок нижнего блока приведён к новому визуальному: прослушивание, его хоткей, обе галочки, повтор последнего, громкость Discord, затем плитки. Размер окна не менялся.
- Вёрстка проверена на шести реальных записях пользователя: снимок `results/ui-recordings.png` показывает все шесть плиток, обе галочки и отсутствие полосы прокрутки страницы.
- Проверки: `bin/effects_check.exe` (обновлён `last_effect_replay`: усиление не пишется, публикация законченной записи и её очистка при чтении), `bin/bridge_check.exe`, Cargo test 27/27 с новым `recordings_keep_six_play_and_save`, строгий Clippy, release-сборка. Обновлён `bin/MicNoize.exe` (SHA-256 `E8B734833539EB69967BA8D7F66722DC630677AE5695D74298EC89A0714083E4`), TAG host не перезапускался. Подставные файлы для проверки ротации удалены. Запись с реальных хоткеев подтверждена: в папке появились шесть файлов пользователя, включая дубликат секунды с суффиксом ` (2)`. Не проверялись: прослушивание записей на слух, оба диалога выбора папки и копирование в саундпад.

## 2026-09-22 — приглушены регуляторы громкости саундпада

- Шкала и процент громкости каждой строки имеют 15 % непрозрачности в покое, а белый бегунок полностью прозрачен; при наведении на строку или сам ползунок всё возвращается к 100 %. Остальная строка, размеры и управление не менялись.
- Добавлено состояние hover с защитой от запоздавшего `on_exit` соседней виртуализированной строки. Прошли focused controller test, `view::tests` 4/4, строгий Clippy и release-сборка. Обновлён `bin/MicNoize.exe` (SHA-256 `98C6508466046C06295650A70AECBD069274B123373F11AF26D2C41A1261314C`), TAG host не перезапускался. Снимок покоя `results/soundpad-volume-hidden-handle.png` проверен; реальное наведение мышью оставлено пользовательскому прогону через `run.ps1`.
- `cargo fmt --check` не чист до этой правки: rustfmt предлагает форматирование уже существующих незакоммиченных блоков в `engine.rs`, `main.rs` и `view.rs`; массовое форматирование не применялось.

## 2026-09-22 — устранено дробление перерисовки строк саундпада

- По сравнению пользователя «Разделы» прокручивались плавно, а «Звук» — нет. Воспроизведено без окна через тот же `damage::diff/group` и CPU rasterizer, что использует compositor Iced. Для 454 клипов, 1100×900, масштаб 100%: до исправления медиана 144 области и 150,07 мс (p95 179,81 мс); после — 1 область и 5,34 мс (p95 6,41 мс). Предыдущий замер только layout и даже полной отрисовки не выявлял эту проблему.
- Строка помещена в штатный `Stack` с отдельным обрезанным слоем. Перемещение инвалидирует полосу строки целиком вместо множества разрозненных надписей/ползунков; damage-группировка объединяет полосы. Tiny-skia, состав контролов и размеры сохранены.
- `soundpad_scroll_timing` теперь обычная синтетическая регрессия на 100 клипов с проверкой числа областей. При `MNR_SCROLL_BENCH_FOLDER=<папка>` выполняет 90 кадров реальной библиотеки; `MNR_SCROLL_BENCH_SCALE=2` — 200%; запуск: Cargo test `soundpad_scroll_timing -- --nocapture`. Тест использует уже имеющиеся транзитивные библиотеки `iced_tiny_skia`/`tiny-skia` как dev-зависимости, без новых runtime-зависимостей.
- Проверки: `view::tests` 4/4, контроллер `soundpad_keys_volumes_and_keyboard` прошёл. При 200%: медиана 16,22 мс, p95 26,67 мс, 1 область. Изображение `results/soundpad-scroll-headless.png` проверено; UI не открывался, мышь/клавиатура не использовались. Это время CPU-отрисовки без оконного compositor ОС, не экранный FPS.
- Выполнена одна финальная release-сборка приложения; `bin/MicNoize.exe` обновлён, хеш проверен. Процессы приложения и TAG host не перезапускались; обновление применяется после полного выхода и запуска `run.ps1`.

## 2026-09-22 — прокрутка по кадрам вместо цепочки таймеров

- Пользователь подтвердил: предыдущая правка виртуализации не устранила видимые лаги. Headless-замер на выбранной библиотеке (454 аудиоклипа): построение view, diff и layout — медиана 0,25 мс, p95 0,40 мс. Это не замер растеризации или экранного FPS. Отдельный замер `Sleep(8)` — в среднем 15,97 мс (30 повторений).
- Убрана цепочка `sleep(8 ms) → ScrollFrame → Probe → ScrollProbe` на каждом шаге. Анимация использует `window::frames`, экспоненциальное сглаживание по прошедшему времени и одну операцию перемещения; геометрия запрашивается при начале жеста. Виртуализированные строки получают смещение в том же обновлении. После остановки подписка снимается, смена страницы/фильтра и потеря фокуса отменяют анимацию.
- Добавлены проверки независимости сглаживания от частоты кадров и контроллера прокрутки (накопление колеса, завершение, отмена устаревшего запроса). Повторяемый headless-замер: `MNR_SCROLL_BENCH_FOLDER=<папка>` и Cargo test `soundpad_scroll_timing -- --ignored --nocapture`. Окна, ввод, аудиофайлы и настройки пользователя не изменялись; экранный FPS требует повторной пользовательской проверки.
- Проверки: фильтр `scroll` — 2 прошли, 1 timing-тест штатно пропущен; `smooth::tests` — 2/2; release-сборка успешна. `bin/MicNoize.exe` обновлён с проверкой хеша, открытый процесс и TAG host не остановлены. Новая версия вступает в силу после полного выхода и повторного `run.ps1`.

## 2026-09-22 — саундпад: фокус больше не раздувает список при прокрутке

- Виртуализация сохраняет отдельно строку с клавиатурным фокусом и строки рядом с видимой областью. Раньше она строила также все строки между ними: после выбора трека стоимость прокрутки росла с расстоянием до него. Промежутки теперь заменены `Space`, положение строк и поиск фокуса сохранены.
- Диапазон ограничен текущей длиной списка: устаревшее смещение после фильтрации больше не приводит к выходу за границы.
- Проверка: `cargo test --release --locked ... view::tests` — 3/3; регрессия на 1000 клипов, фокус по обе стороны окна, отсутствие дублей и сокращение списка. В сценарии с фокусом на первой строке строятся 27 строк вместо 423. Живой FPS не измерялся; окна и ввод пользователя не использовались.
- Release-сборка прошла; обновлён `bin/MicNoize.exe`, SHA-256 совпадает со сборкой. Приложение не запускалось, TAG host не перезапускался.

## 2026-09-22 — саундпад: разделы слева, перетаскивание, прокрутка ещё легче

- **Хоткеи назначаются на месте.** Отдельная страница назначения удалена (`binding_view`, `focus::bind`): кнопка хоткея сама переходит в режим захвата («Нажмите…», оранжевая рамка), первая допустимая клавиша принимается сразу (`Tick` → `AcceptBind`), конфликт остаётся на кнопке красным («Занято: F12») и захват перезапускается, повторный клик или Esc отменяет, × рядом с кнопкой убирает назначение. Вкладки и остальной UI не блокируются; смена вкладки отменяет захват. Применено ко всем 13 привязкам эффектов, стоп-клавише и строкам саундпада (`bind_button`, `bind_focus`). Проверено живьём: назначение F12, конфликт «Занято: F12» на второй строке, затем F7 принят, × снял оба.
- Окно быстрого двойного нажатия для перезапуска клипа уменьшено с 350 до 170 мс по запросу пользователя (`soundDoublePressMs`, `audio.hpp`); `effects_check` и `bridge_check` перестроены и прошли.

- **Разделы** (левая колонка вкладки): «Все звуки», автоматические группы по префиксу имени до « - » (`soundpad::groups`, от двух файлов, по размеру), свои разделы (`Section`, `[soundpad] sections` = `имя:файл|файл` через табуляцию, дубли имён и пустые имена отбрасываются при загрузке). «+ Раздел» создаёт и сразу даёт ввести имя (пустой ввод оставляет прежнее, дубль отклоняется с подсказкой), «Удалить раздел». Выбор раздела фильтрует список и сбрасывает его прокрутку.
- **Перетаскивание**: ручка ≡ в начале строки (`mouse_area.on_press` → `DragStart`), записи разделов сообщают наведение (`DragOver`), глобальный `mouse::ButtonReleased` в подписке завершает бросок (`DragEnd`); во время перетаскивания заголовок колонки меняется на «Отпустите на разделе», цель подсвечивается оранжевой рамкой. × в строке выбранного своего раздела убирает звук.
- **Прокрутка**: вкладка больше не внутри общей прокрутки страницы — панель, поиск и заголовок закреплены, список в собственном `scrollable("body")` с `on_scroll`, поэтому окно виртуализации считается от нуля с запасом 96 px (≈20 строк вместо ≈50), а `keyed::Column` сохраняет состояние строк (shaped text) при сдвиге окна. Список разделов — свой `scrollable("sections")`. Клавиатура: разделы, «+ Раздел», имя и удаление включены в порядок Tab (`SECTION_BASE + i`, 69–71).
- Проверки: Rust 21/21 (контроллер: создание/переименование/дубль/бросок/бросок мимо/убрать/удалить/сохранение разделов; модель: `prefix`, `groups`, `sections`, `serialize_sections`, `section_name`), строгий Clippy. Живой прогон в `bin\MicNoize.exe` на `E:\Dropbox\sounds`: 17 автогрупп (gerych 86, nastya 57, seriy 47 …), клик по группе фильтрует список, «+ Раздел» + имя, перетаскивание `1.mp3` за ≡ на раздел с подсветкой цели и подтверждением ««1.mp3» добавлен в …». FPS прокрутки на глаз не измерен — нужен отзыв пользователя.

## 2026-09-22 — саундпад: прослушивание, прокрутка, сортировка

- **«Слышать звуки в наушниках» не работало.** `Engine::preview` добавлял сэмпл клипа в значение, но потребитель `Monitor` повторно фильтрует очередь по категории сэмпла (`previewSample`), а бит `ModifiedSound` на сэмплах не ставился — клип отбрасывался. Теперь сэмпл preview-очереди с клипом получает `ModifiedSound` (только там; `RoutedSample` по-прежнему без него). Логика вынесена в `previewQueued` (`audio.hpp`), `effects_check` прогоняет её через потребительский `previewSample`: маски 4–7 пропускают клип, маски без бита 4 и Mute — нет.
- **Лаг прокрутки.** Папка пользователя — 454 клипа; tiny-skia перерисовывал все строки на каждом тике (50 мс). Список виртуализирован: `scrollable("body").on_scroll` сообщает смещение и высоту окна, строки фиксированной высоты 30 + 2 px, строятся только строки в пределах ±420 px от видимой области плюс строка с фокусом клавиатуры; остальное — два `Space`. Порядок Tab и `reveal_focus` сохранены.
- **Сортировка** (`[soundpad] sort`): по имени, с хоткеем сверху (в порядке клавиш), недавно игравшие, новые файлы. Время последнего запуска берётся из состояния движка (покрывает и хоткеи) и сохраняется четвёртым полем записи `sounds` (`key:volume:played:name`; трёхполевые записи первой сборки читаются). Добавлен `m4a` (symphonia `aac` + `isomp4`).
- Громкость саундпада: по отзыву пользователя клипы шли в разы громче голоса и эффектов (нормализованные mp3 ≈ 0 dBFS против речи ≈ −18 dBFS). Отображаемые 100 % теперь физический gain 0,2 (`SOUND_VOLUME_AT_100`), 200 % — 0,4; настройка в процентах не изменилась, per-clip 0–200 % умножается сверху.
- Под галкой «Слышать звуки в наушниках» показывается, что делает монитор: устройство вывода и уровень того, что он реально отрисовал (`mnr_monitor_peak`, удерживаемый пик `Monitor::renderedPeak`), «тишина» / «−N dBFS», «Подключение…», ошибка или «включится вместе с обработкой».
- Живая проверка в запущенном `bin\MicNoize.exe` (маршрут QuadCast → NVIDIA v2 → TAG, папка `E:\Dropbox\sounds`, 454 клипа): прокрутка колесом на 60 тиков и обратно — строки строятся мгновенно; клик по клипу `1` (mp3) — строка оранжевая, `0:02 / 1:30`, индикатор монитора «Speakers (6- SMSL USB DAC) · −15…−18 dBFS» во время клипа и «тишина» до и после «Стоп»; сортировка переключается и сохраняется. Loopback-запись выхода (`results/soundpad-loopback-peak*.log`) не изолирует монитор от другого звука на DAC и не используется как доказательство.
- Проверки: `mic_engine` пересобран, Rust 21/21 (обновлены `scan_decode_and_persist` — 4-полевые записи, `order`; `soundpad_keys_volumes_and_keyboard` — фокус сортировки, порядок «недавние», сохранение `sort`), строгий Clippy чисто; снимок `results/soundpad-ui-real.png` на реальной папке `E:\Dropbox\sounds` (454 клипа, режим «С хоткеем сверху»). Не проверены: субъективная слышимость (монитор отрисовывает клип на устройство наушников, ушами не подтверждено), m4a на живых файлах.

## 2026-09-22 — вкладка «Саундпад»

- Новая вкладка: папка-хранилище звуков (`mp3`/`wav`/`ogg`), импорт файлов копированием, поиск, у каждой строки запуск/стоп, громкость 0–200 % и хоткей; общая громкость, хоткей и кнопка «Остановить всё», опция «Слышать звуки в наушниках». Настройки в `[soundpad]`, привязки по имени файла. Диалоги выбора папки/файлов — нативный `IFileOpenDialog` (`mnr_pick_paths`), декодирование — `symphonia 0.6` в Rust (новая зависимость, `Cargo.lock` обновлён), линейная передискретизация в 48 кГц моно.
- Движок: `SoundClip`/`SoundPlayer` в `audio.hpp`; клипы хранятся под мьютексом, DSP-поток только `try_lock`; один клип за раз, второе нажатие — стоп, быстрое двойное (350 мс) — перезапуск, затухание 240 сэмплов. `RoutedSample.sound` проходит очередь отдельно; `OutputEffects::process` получил параметр `sound` и подмешивает его после Discord-gain и до фонового микрофона в TAG и WASAPI. Маска монитора расширена битом 4 (`ModifiedSound`, только маска): режимы `mnr_monitor` 0–8. Хоткеи звуков опрашиваются в shell-потоке отдельно от `HoldLatch` с той же `eligible`-логикой; C ABI: `mnr_sound_load/gain/clear/play/volume/bindings/state`.
- Проверки: `effects_check` (новый блок: пуск, громкость, перезапуск с fade, стоп, окончание, обход Discord-gain и preview) и `bridge_check` (валидация клипов, коллизии хоткеев со связками эффектов, маска 8, режим 9 отклонён) прошли; `effects_check` теперь держит тестовый `Engine` в куче — кольца выросли до ~800 КБ и переполняли стек по умолчанию. Rust: 21 тест (новые `soundpad::scan_decode_and_persist` — WAV 44,1 кГц стерео → 48 000 сэмплов, `controller_tests::soundpad_keys_volumes_and_keyboard` — порядок Tab, отказ дублей с эффектами/стопом/другим звуком, сохранение); `verify.ps1` прошёл (CTest 3/3, Rust 21/21, строгий Clippy); логи `results/soundpad-*.log`. Собранный UI установлен в `bin\MicNoize.exe`; TAG-хост не трогался. Снимок `results/soundpad-ui.png` (`--ui-soundpad`) осмотрен. Не проверены: физические хоткеи и реальное воспроизведение в Discord, mp3/ogg на живых файлах (тест декодера использует WAV), прослушивание человеком, нагрузка при сотнях клипов.

## 2026-09-22 — новая шкала громкости Discord

- Product defaults подтверждены и закреплены тестом: усиление 300 %, перегрузка выключена. Шкала общей громкости пяти Discord-эффектов теперь 0–200 %: новые 100 % дают прежний физический gain 0,08, новые 200 % — 0,16. Старое сохранённое `discord_volume=8` без маркера шкалы загружается как 100 % без изменения слышимой громкости; последующие сохранения получают `discord_volume_scale=2`.
- Предел 0,16 применяется на C ABI boundary и общий `OutputEffects` остаётся после усиления/перегрузки только для Discord-сэмплов, поэтому одинаково действует в TAG и WASAPI и не масштабирует микрофон. `verify.ps1` прошёл: CTest 3/3, Rust 18/18, Clippy; лог `results/discord-volume-scale-verify.log`. Субъективное прослушивание в Discord не проводилось.

## 2026-09-22 — Mic Noize 0.2.0: публикация и контрольная установка

- Репозитории переименованы в public `Arkanoidvfx/MicNoize` и private `Arkanoidvfx/MicNoize-dev`. Telemetry Worker принимает новый `mic_noize`, сохраняет переходный `mic_noise_reducer` и fallback на прежний секрет; vitest 91/91 и typecheck прошли, deploy run `35662099192` зелёный.
- Опубликован и проверен `runtime-core-v2`: Ed25519-подпись валидна, размер `core-runtime-2.part000` в GitHub и манифесте совпадает (1 153 997 586 байт), архив содержит новый `mic_tag_host.exe`. Манифест `runtime-rvc-v2.1.4` переподписан с URL `Arkanoidvfx/MicNoize`; подпись, размеры и SHA-256 двух существующих частей совпали.
- Release run `35663002046` прошёл и опубликовал `v0.2.0`. Скачанный `Mic-Noize-Setup-0.2.0.exe` совпал с `checksums.sha256` (`EA9D4E6DDF5363BFF8E955641C09101442E42E04FD320715F7D8E665E752F4B2`); метаданные показывают `Mic Noize` / `0.2.0`, Velopack package id — `MicNoize`.
- Установка из Проводника создала `%LOCALAPPDATA%\MicNoize\current\MicNoize.exe`; UI 0.2.0 загрузил NVIDIA DLL из `%APPDATA%\Mic Noize\Components`, сохранил прежний `settings.ini` и поднял сохранённый маршрут `QuadCast S → NVIDIA v2 → TAG`. После удаления приложения install root и UI исчезли, но `settings.ini`, `install-id.txt`, runtime v2 и TAG endpoint **Mic Noize (Thin Audio Gateway)** остались; SHA-256 настроек не изменился. Главная цель отделения пользовательских данных от программы подтверждена.
- Старые `v0.1.0–v0.1.2` и `runtime-core-v1` помечены pre-release как superseded; RVC, core v2 и v0.2.0 остаются обычными релизами. Не проверены наушниковая линия, SmartScreen на чистом ПК и субъективное прослушивание установленной сборки.

## 2026-09-21 — Mic Noize 0.2.0: локальная миграция TAG

- Старый хост PID 16932 остановлен только после выхода UI; новый `mic_tag_host.exe` собран и запущен из Проводника, PID 34508. Миграция оставила один активный capture endpoint: **Mic Noize (Thin Audio Gateway)**, id `{0.0.1.00000000}.{e4ba2cb9-afc9-47ed-909f-20513051c269}`. `Thin Audio Gateway` — имя подписанного родительского драйвера из INF; KS-линия называется `MicNoize Microphone`.
- `mic_check --tag-reconnect 4`: стойла 120/400 мс пережиты, `reconnects=1/2`, `underruns=0`, `drops=0`, `TAG_gaps=0`. `--persistent-tag 4`: тот же endpoint, постоянно открытый клиент и два жизненных цикла движка прошли. Логи: `results/mic-noize-0.2.0-tag-reconnect.log`, `results/mic-noize-0.2.0-persistent-tag.log`. VRAM preflight: 15 223 MiB свободно из 24 576 MiB.
- UI `bin\MicNoize.exe` запущен из Проводника, маршрут `QuadCast S → NVIDIA v2 → TAG` поднялся на repository runtime (в процессе загружены `NVAudioEffects.dll` и `nvafxdenoiser.dll`). `%APPDATA%\Mic Noize\settings.ini` побайтно совпал с legacy-файлом; старые Components не копировались, dev runtime видит 6 repository RVC-слотов. Старый `%LOCALAPPDATA%\MicNoiseReducer` содержал только эту копию настроек и восстановимо перенесён в `.tmp\archive\LocalAppData-MicNoiseReducer`.
- Реальный HKCU Run нельзя достоверно прочитать из MSIX-терминала агента: его виртуализированный view всё ещё показывает legacy-значение; успешный внешний запуск нового хоста подтверждён новым PID и endpoint. Ещё не проверены наушниковая линия, установка/удаление 0.2.0 и публикация.

## 2026-09-21 — Mic Noize 0.2.0: внутренний rename, шаг 1

- Пакет и dev EXE переименованы в `micnoize` / `bin\MicNoize.exe`; удалён старый Win32 UI и его CMake target. Velopack теперь использует `packId MicNoize`, `MicNoize.exe` и версию 0.2.0; URL приложения и runtime переведены на `Arkanoidvfx/MicNoize`, core manifest — на `runtime-core-v2`.
- Постоянные данные перенесены в `%APPDATA%\Mic Noize`; `settings.ini` при отсутствии копируется сначала из legacy `%LOCALAPPDATA%\MicNoiseReducer`, затем из dev-репозитория. Dev-сборка всегда использует repository `vendor`, установленная — `%APPDATA%\Mic Noize\Components`. `MNR` трактуется как Mic Noize Runtime; ABI, переменные окружения, sidecar и IPC `.v1` не менялись.
- Win32 mutex/mapping/window-class и TAG-линии переименованы в `MicNoize.*`; новый хост создаёт `MicNoize Microphone` до попытки удалить legacy-линии, а ошибки удаления пишет в `results/tag-host.log`. Автозапуск мигрирует `MicNoiseReducer.TagHost` в `MicNoize.TagHost`.
- `verify.ps1` прошёл: CTest 3/3, Rust 17/17, строгий Clippy; лог `results/mic-noize-0.2.0-step1-verify.log`. Собран `bin\MicNoize.exe`; старые dev EXE восстановимо перенесены в `.tmp\archive`. Живой legacy `mic_tag_host.exe` не останавливался и не заменялся. Не проверены: сборка/запуск нового хоста, миграция физических TAG endpoint, Discord, аппаратные `--tag-reconnect`/`--persistent-tag`, установка/удаление 0.2.0, telemetry worker и публикация.

## 2026-09-21 — проверка обновления 0.1.1 → 0.1.2 на этом ПК

Первая проверка полного пользовательского пути с опубликованными артефактами, без песочницы агентов (Setup и приложение запускал пользователь из Проводника; см. заметку о MSIX-виртуализации в `workflow.md`).

- `Mic-Noize-Setup-0.1.1.exe` скачан с GitHub, SHA-256 совпал с `checksums.sha256` релиза; на файл поставлен Mark-of-the-Web. Установка Velopack в `%LOCALAPPDATA%\MicNoiseReducer` за секунды: `current\`, `Update.exe`, `packages\`, ярлыки «Mic Noize» в меню Пуск и на рабочем столе, запись «Mic Noize» / Arkanoid VFX в «Приложениях». Показ SmartScreen пользователь не запомнил — не зафиксирован.
- 0.1.1 при первом запуске сразу нашла и скачала пакет 0.1.2 (6.2 МБ, через 5 с после установки) и параллельно скачала core runtime 1.15 ГБ, после чего показала ожидаемую ошибку «Архив компонента не содержит ожидаемый файл» (баг проверки пути в 0.1.1). Части остались в `Components\.download-rvc`.
- «Обновить сейчас» через ~2 минуты после установки: приложение вышло, `Update.exe` подменил `current\` на 0.1.2 и перезапустил его; `install-id.txt` и `settings.ini` сохранены; запись в «Приложениях» обновилась до 0.1.2.
- 0.1.2 установила core runtime за 7 с, переиспользовав скачанные части без повторной загрузки; временные каталоги удалены; маршрут стартовал сам (QuadCast → NVIDIA v2 → Thin Audio Gateway), голос слышен, `sessions.log`: 20 230 блоков, 0 underruns/drops, TAG gaps 0. Второй `mic_tag_host` не поднимался: установленное приложение работало с хостом из `bin` через существующий mapping.
- **Деинсталляция (`Update.exe --uninstall`) удаляет всю папку `%LOCALAPPDATA%\MicNoiseReducer`** — вместе с `settings.ini`, `install-id.txt` и `Components` (1.7 ГБ). Ярлыки и запись в «Приложениях» убраны, автозапуск хоста (`HKCU\…\Run`) не тронут. Следствие: у пользователя при удалении программы погибнут импортированные RVC-модели и настройки — данные надо отселить из корня установки (открытый пункт, код не менялся).
- Косметика: кнопка «Обновить сейчас» отрисована как активная и при «Установлена актуальная версия» (нажатие отключено).
- `Run.bat` не запускался из Проводника: в системном `PATH` этого ПК отсутствует `C:\Windows\System32\WindowsPowerShell1.0`. Теперь вызывает `powershell.exe` по полному пути и держит окно при ошибке. Dev-сборка после теста запущена из Проводника: настройки мигрировали в `%LOCALAPPDATA%\MicNoiseReducer\settings.ini`, runtime из `vendor` репо, хост прежний.
- Git: `main` = ветка Codex (fast-forward), приватный `origin` догнан, теги `v0.1.x` получены из публичного репо; процедура экспорта документирована. Не проверено: установка на чистом ПК, SmartScreen как таковой, обновление между двумя *будущими* версиями с delta-пакетом.

## 2026-09-21 — Mic Noize 0.1.2

- Исправлена установка основного runtime: архив содержал правильный `vendor/nvidia-afx-3.0.0/bin/NVAudioEffects.dll`, но приложение проверяло несуществующий путь внутри `features`. Та же ошибка заставляла считать уже установленный core отсутствующим.
- Валидная скачанная часть теперь повторно используется после ошибки с обязательной проверкой размера и SHA-256, поэтому повторная попытка не загружает 1.15 ГБ заново.
- Полный сценарий первого запуска проверен на опубликованном `runtime-core-v1`: NVIDIA/TAG файлы установились, временные каталоги удалены, UI показал `Основной runtime 1 установлен`; `mic_tag_host.exe` сохранил PID 16932.
- `verify.ps1`: 3/3 native CTest и 17/17 Rust tests прошли. Локальный Setup 0.1.2 собран, метаданны показывают `Mic Noize` / `0.1.2`, голосовых моделей в пакете нет.
- GitHub Actions run `35626036201` прошёл и опубликовал `v0.1.2`. Скачанный из release `Setup.exe` имеет SHA-256 `65E92E2E98E9F4C3BD10FF1B03ACF4F068C90D7E877BC7F091575C1FC4DD0501`, совпадающий с `checksums.sha256`; метаданны `Mic Noize` / `0.1.2`, голосовых моделей в full package нет.

## 2026-09-21 — Mic Noize 0.1.1

- Видимое название приложения, окна, трея, EXE-метаданных, установщика, release workflow и информационных файлов изменено на **Mic Noize**.
- Внутренние `MicNoiseReducer` package ID, имена бинарников, `%LOCALAPPDATA%`, URL репозитория, mutex/window-class и TAG endpoint сохранены для бесшовного обновления 0.1.0 → 0.1.1 без потери настроек и без создания второй виртуальной линии.
- Проверены CTest 3/3, 16 Rust-тестов, строгий Clippy, release-сборка и Velopack-упаковка; Windows-метаданные EXE и `Setup.exe` показывают `ProductName=Mic Noize`, `FileDescription=Mic Noize`, версию 0.1.1. SHA-256 локального `Setup.exe`: `5F0586C7335CE1EBD1550F58221CE115759E2320C16879C49949F19A9FF828B4`. Повторная упаковка больше не пытается хэшировать собственный `checksums.sha256`. TAG-хост не перезапускался.
- GitHub-hosted workflow полностью прошёл и опубликовал `v0.1.1`; скачанный обратно `Setup.exe` имеет имя/версию Mic Noize 0.1.1 и совпадает с SHA-256 `F0D33575C7F6FA881425652A091AF9F9D64A138A346B991131C73C3C81057119`. Velopack manifest содержит пакет `MicNoiseReducer` 0.1.1, сохраняя цепочку обновлений. Интерактивный переход установленной 0.1.0 → 0.1.1 ещё не проверялся.
- QA-снимок настроек подтвердил `Mic Noize` в шапке и `Mic Noize 0.1.1` в версии (`results/mic-noize-0.1.1.png`). Локальный `bin\MicNoiseReducer-rust.exe` заменён проверенной сборкой 0.1.1 и запущен снова; `mic_tag_host.exe` сохранил прежний процесс и не перезапускался.

## 2026-09-21 — Setup, обновления и отделённые runtime-компоненты

- Добавлена поставка через Velopack: `Setup.exe`, фоновая проверка/загрузка версии и применение обновления при следующем запуске. Пользовательские данные перенесены в `%LOCALAPPDATA%\MicNoiseReducer`.
- NVIDIA/TAG и RVC вынесены в подписанные Ed25519 runtime-релизы с SHA-256. Основной runtime ставится при первом запуске, RVC — при первом включении. Пользовательские `.pth`, `.index`, `.onnx` и каталоги моделей исключены из упаковки.
- Добавлена минимальная HMAC-телеметрия жизненного цикла и аудиосчётчиков в общий Moment Player Worker с отдельным `app_id` и секретом. Публичный исходный репозиторий и release workflows подготовлены; Authenticode не используется.
- Проверены CTest 3/3, 16 Rust-тестов, строгий Clippy, Worker 90/90 и production ingest HTTP 202. Опубликованы Setup v0.1.0, core 1, RVC 2.1.4; удалённые манифесты совпали с локальными, RVC-архив содержит 4 819 файлов и 0 пользовательских моделей. Clean-PC установка, SmartScreen, физическое прослушивание и переход между двумя опубликованными версиями остаются непроверенными.
- GitHub-hosted release workflow на `windows-2022` прошёл полностью: восстановил runtime, собрал native/Rust, выполнил проверки, сформировал Velopack и обновил релиз v0.1.0. Скачанный из релиза `Setup.exe` совпал с опубликованным SHA-256 `988DD1DAE8CAB33AC22DBD58ED19ED5D7258D6D1B0FF5A5F914F32A6FC98865A`.

## 2026-09-20 — исправления по ревью

Ревью кода дало оценку 63/100 и семь пунктов; исправлены пп. 3–7 (пп. 1 — Git, 2 — Mute/Start/Stop снят пользователем). Репозиторий: `Arkanoidvfx/MicNoise`, ветка `main`; в Git только исходники, скрипты и документация.

- **verify.ps1** теперь сначала пересобирает `mic_engine`, `mic_check`, `effects_check`, `bridge_check` (хост и UI не трогает), затем CTest, Rust-тесты и Clippy `--all-targets`. До этого проверочные exe были старее движка. Baseline после правки: CTest 3/3, 14 Rust-тестов, Clippy чисто (`results/verify-step1.log`).
- `tag_host_autostart()` запускает `reg query` с `CREATE_NO_WINDOW` — раньше при каждом старте UI мигало консольное окно. `Run.bat` теперь вызывает `run.ps1` через `powershell -File`, а не открывает его.
- **Переподключение TAG.** Хост сбрасывает производителя через 50 мс без данных и заполняет паузу тишиной; раньше следующая запись клиента получала отказ и сессия падала с «restart processing». `TagClient::write` при первом отказе выбрасывает блок с накопленным долгом часов, заново регистрируется (`command(1)`) и возвращает `false`; `tagLoop` сбрасывает часы и приминг как при смене клиента. Два отказа подряд по-прежнему ошибка. Новые `Stats::tagReconnects` (в `sessions.log` поле `reconnects=`) и тест-хук `Engine::testStallMs`. Проверка `mic_check --tag-reconnect 2` с живым хостом: стойла 120 и 400 мс пережиты, `reconnects=1/2`, приёмник не терял поток (69–97 тыс. кадров на фазу), `TAG_gaps=0`, `underruns=0`, `drops=0`; разрывы захвата WASAPI (3 и 5) ожидаемы при паузе >20 мс. Регрессия `--persistent-tag 2` прошла. Логи: `results/tag-reconnect-check.log`, `results/persistent-tag-after-reconnect.log`. Первые ~50 мс дыры до срабатывания watchdog хоста никто не заполняет — одна короткая пауза вместо мёртвой сессии; стойло >2 с по-прежнему роняет сессию (watchdog микрофона). `HeadphoneClient` с порогом 2 с и авто-рестарт движка после ошибки не менялись.
- **Именованные константы C++.** `enum Hold` (`HoldBoost..HoldReverse`, `HoldPhrases`, `HoldLive`, `DiscordShift`, маски) и `enum Modified` в `hotkeys.hpp`; `PhraseEffect::State` с прежними числовыми значениями (ABI и Rust UI остаются числовыми). Вывод `effects_check` побайтно совпал с эталоном без полей времени (`results/effects-check-before-constants.norm.log`, `-after-`).
- **RVC: фиксированная задержка.** Три дефекта прежнего `RvcClient`: до первого ответа шёл сырой голос, затем та же речь преобразованной (дубль); каждый underrun навсегда добавлял 10 мс задержки; любой Discord-хоткей выключал RVC для своего голоса и требовал холодного рестарта. Теперь `RvcPlayout` — линия задержки `блок + 200 мс` (`rvcSlack`): входной отсчёт j слышен строго на j+D, опоздавшее отбрасывается, отсутствующее — **тишина, не сырой голос** (решение пользователя). Оба кольца тегируются поколением и выровнены по индексам; worker выдаёт ровно один выходной отсчёт на входной (нули при пропуске чанка, отставшего на ≥ 200 мс, и при ошибке сервера), после пропуска меняет stream id, чтобы сервер сбросил crossfade. Только DSP-поток подрезает `output_`. `rvc->process` перенесён до подмены источника Discord: pitch, фразы и повтор работают с преобразованным голосом, при Discord-удержании свой голос остаётся преобразованным. Mute = выключение RVC ⇒ после unmute D тишины. Проверки: `effects_check` (детерминированный тест playout: прогрев, точная задержка, частичный/полный underrun, отбрасывание опоздавшего без сдвига задержки, чужое поколение, сброс), `bridge_check`, `mic_check --rvc-check` с живым сайдкаром: 40 блоков точной тишины, затем 280/280 блоков преобразованы, 0 тихих; `rvc_check.py`: inference после прогрева 57–79 мс для блоков 100–500 мс, пики 75–244 мс (`results/rvc-playout-check.log`). Прослушивание человеком не проводилось. Зависший (не упавший) сервер даёт тишину с состоянием «пауза» до выключения; упавший UI выключает сам через ~250 мс.
- **Именованные focus-ID Rust.** `mod focus` группирует ~60 идентификаторов по страницам с прежними значениями; тесты контроллера оставлены числовыми намеренно. Снимки четырёх страниц до/после совпали побайтно (`results/focus-snapshots/`). Удалён недостижимый обработчик `6 => Msg::Settings` на главной странице.
- README разделён: поведение — в `README.md`, история — здесь. Указатели в `AGENTS.md`, `SKILL.md`, `workflow.md` обновлены.

Не проверено в этой серии: поведение драйвера TAG при заморозке позиции на ~50 мс на слух; бюджет 200 мс при блоке 500 мс под игрой (синтетика: пик 137 мс); качество RVC на реальной речи; отсутствие мигания консоли (проверяется при следующем запуске UI).

## 2026-09-18

### Компактный интерфейс

Фоновый UI: при потере фокуса, сворачивании или скрытии в трей опрос индикатора уровня и его затухание отключены; служебный цикл UI — раз в 250 мс вместо 50 мс при активном окне и работающем тракте. Аудио, native-опрос хоткеев и heartbeat продолжаются независимо. После возврата фокуса индикатор возобновляется. Удалены подписи про микрофон/Discord под реверсом и про задержку прослушивания. Проверены 6 controller-тестов, Release-сборка и снимок (`results/background-ui-tests.log`, `results/background-ui-build.log`, `results/background-ui.png`); замер FPS не проводился.

Слева расположены вкладки «Микрофон», «Наушники», «Voice Changer»; «Настройки» прижаты к правому краю той же строки. Tab следует этому порядку. Перестановка проверена 5 controller-тестами, Release-сборкой и снимком `results/tabs-order-ui.png` (`results/tabs-order-tests.log`, `results/tabs-order-build.log`); обновлён установленный UI.

Проверка первого варианта с вкладками 2026-09-18: Release-сборка и 8 Rust-тестов прошли (`results/ux-build.log`, `results/ux-tests.log`), проверены снимки эффектов, RVC, настроек, окна 620×440 и масштаба 200% (`results/ux-*.png`). Последующая компактная таблица проверяется отдельно (`results/ux-compact-tests.log`, `results/ux-compact-build.log`); старые снимки её не подтверждают. Новая сборка подготовлена в `bin/MicNoiseReducer-next.exe`, но не запущена и не заменяет работающий UI: пользователь запретил переключать окна во время игры. Визуальная проверка таблицы и FPS прокрутки остаются непроверенными. TAG-хост не перезапускался.

Обновление элементов управления: колонки хоткеев сужены до 150 px, «Перегрузка» перенесена в строку усиления; обычный статус обработки и подсказки удержания/источника убраны. Вкладка наушников использует те же цвета, ползунки и разделители; физический выход выбирается кликом по названию, активный тракт перезапускается при смене. Ошибки остаются видимыми. Проверены Release-сборка, 4 controller-теста и `effects_check`, включая точное усиление ×12 до клиппинга (`results/controls-*.log`). Осмотрены снимки главного экрана, наушников и масштаба 200% (`results/controls-main.png`, `results/controls-headphones.png`, `results/controls-scale2.png`). Установлен `bin/MicNoiseReducer-rust.exe`; TAG-хост не перезапускался. Субъективное прослушивание перегрузки и физическое переключение устройств не проверялись.

### Удаление VB-CABLE

Удалены специальное подключение прослушивания через VB-CABLE и разрешение его автозапуска. Release установлен в фоне; bridge_check и 2 controller tests прошли. TAG-хост сохранён. Защита от виртуальных петель, WASAPI и исторические измерения сохранены. Драйвер VB-CABLE удалён через PnPUtil (код 0); устройства CABLE исчезли, TAG остаётся доступным. VoiceMeeter сохранён. Лог: `results/cable-removal-driver.log`.

### Наушники

Проверка диапазона и загрузки сохранённых значений выше 100%: `results/headphones-200-tests.log`; Release обновлён без перезапуска TAG-хоста. Для установки функции хост был однократно обновлён и перезапущен (`HeadphoneLink.v1`); обычные будущие изменения UI не требуют его перезапуска.

Проверка 2026-09-18: Release C++/Rust, `bridge_check`, 3 controller tests; синтетический стереопоток через новый TAG render в WASAPI с Mute, без NVIDIA и с двумя NVIDIA-моделями — 300/299 блоков, 0 переполнений очереди. Проверено прекращение обработки после Stop. Это не прослушивание и не длительный тест задержки/пропусков. Отчёты: `results/headphones-*-check.log`, `results/headphones-tests.log`; снимок вкладки: `results/headphones-ui.png`.

### Повтор последнего

Проверка 2026-09-18: `effects_check` (живой микрофон при записи скорости, общий повтор/замена/лимит/отмена, раздельное смешивание и прослушивание), `bridge_check` и два Rust controller tests прошли. Release установлен; новая строка проверена на `results/replay-ui.png`. TAG host не перезапускался. Общая обработка применяется в TAG и WASAPI; физический вывод и прослушивание человеком в этом изменении не тестировались.

### Громкость эффектов Discord

Проверки: `results/discord-volume-check.log`, `results/discord-volume-controller-check.log`.

### Discord-хоткеи

Логи: `results/discord-capture-check.log`, `results/discord-effects-check.log`, `results/discord-controller-check.log`. Проверены десять удержаний, выбор источника, отсутствие старого хвоста pitch, дубликаты и навигация. Реальный звонок и физические хоткеи ещё требуют пользовательского прослушивания.

### Бережная подрезка реверса

Проверка подрезки 2026-09-18: пересобраны `mic_engine` и `effects_check`; `effects_check` прошёл, включая точное сравнение сэмплов, оба источника, блоки 17/480/997 сэмплов, повтор, границы порога, неполные окна, тихие окончания, внутренние паузы, лимит записи и отмену. Лог: `results/reverse-trim-check.log`. Release UI собран, установлен (SHA-256 совпадает со сборкой) и перезапущен для пользовательского прослушивания; TAG host не перезапускался. Предыдущий EXE сохранён в `.tmp/MicNoiseReducer-before-reverse-trim.exe`. Прослушивание человеком и физические TAG/WASAPI-выходы ещё не проверены; требуется сравнение на речи и шёпоте.

### Импорт модели RVC

Проверка импорта: `src/rvc_import_check.py` — реальная модель, схема VCClient, v1 без F0, кириллица и `.index`, повторные слоты и отказ на неверных файлах. Упакованный импорт проверен через стандартный диалог Windows (отмена и реальная `.pth`); тестовый слот удалён. 7 controller-тестов, Release-сборка и осмотр 100%/200% прошли (`results/rvc-import-*.log`, `results/rvc-import-ui.png`, `results/rvc-import-scale2.png`). Это проверка импорта, без оценки звучания новой модели.

### Второй уровень шумоподавления

Проверка: `effects_check`, `bridge_check`, 7 controller-тестов (включая сохранение/загрузку файла), Release-сборка и осмотр UI 100%/200% прошли (`results/noise-hold-*.log`, `results/noise-hold-ui.png`, `results/noise-hold-scale2.png`). Установленная сборка обновлена; TAG-хост не перезапускался. Физическое удержание клавиши и субъективное звучание не проверялись.

### OpenMP и порт RVC

В установленном FAISS исправлен конфликт OpenMP с PyTorch: оба `_swigfaiss*.pyd` используют `torch/lib/libiomp5md.dll`; совместимость импортируемых функций проверяет `src/rvc_openmp_fix.py` (запуск из `.tmp/pyi310-venv`). Оригиналы сохранены в `.tmp/rvc-openmp-originals`. После замены VCClient нужно повторить исправление. Проверка PyTorch + FAISS add/search в одном процессе прошла; полный запуск RVC после исправления пока не проверен, поскольку запуск диагностического сервера заблокировала автоматическая проверка разрешений. `KMP_DUPLICATE_LIB_OK` не используется.

Повторная проверка исправления OpenMP: упакованный сервер загрузил `GoogleRus` (слот 3), индекс 100%; три синтетических блока по 200 мс прошли преобразование с проверкой размера и конечных ограниченных значений PCM (54–74 мс обработки). Внутренний адрес RVC перенесён на `127.0.0.1:18889` одновременно в сервере, движке и проверке: старый порт удерживался завершённым процессом. Пересобраны сервер, `mic_engine` и UI; установлено обновление. Проверка звучания человеком не проводилась.

Диагностика хрипа RVC на синтетическом сигнале: 32 блока GoogleRus (тон 0/+9, блок 200/300 мс, индекс 0%) без клиппинга и без дополнения HTTP-ответов нулями. В коде обнаружен резкий переход к сухому голосу при опустошении очереди RVC; связь с наблюдаемым хрипом пока не подтверждена. Ограничения и измерения: `results/rvc-quality-review.md`, `results/rvc-quality-probe.json`. (Механизм устранён 2026-09-20 — см. выше.)

### Прослушивание эффектов усиления

«Слышать результат эффектов» (`[effects] monitor_effects`) и независимая галка «Слышать результат эффекта усиления» (`monitor_boost`). Проверены `effects_check`, `bridge_check`, 5 controller-тестов и Release-сборка (`results/boost-monitor-*.log`); осмотрены снимки 100% и 200% (`results/boost-monitor-ui.png`, `results/boost-monitor-scale2.png`). Обновлён UI, TAG-хост не перезапускался. Физическое прослушивание не выполнялось.

### Закрытие крестиком

Обработчик скрывает существующее окно через `Mode::Hidden`, а открытие из трея восстанавливает его через `Mode::Windowed`, без уничтожения окна. Фоновый heartbeat сохраняется, опрос индикаторов при скрытом окне отключён. Включено в установленную Release-сборку 2026-09-18; тесты не запускались по просьбе пользователя, устранение сообщённого сбоя пока не подтверждено.

### Отключение RVC

Проверка отключения RVC (2026-09-18): `bridge_check` подтвердил dry bypass выключенного RVC, завершение спящего worker, ABI/Mute; Rust-тест отключает скрытый тестовый процесс через рабочую ветку toggle-off без периода ожидания. Все 8 Rust-тестов и Release-сборка прошли (`results/rvc-idle-check.log`, `results/rvc-off-tests.log`, `results/rvc-off-build.log`). Обновлён `bin/MicNoiseReducer-next.exe`; работающая версия не перезапускалась. Реальная загрузка/выгрузка модели и замер освобождённой VRAM не проводились во время игры.

## 2026-09-17

- Ревью: исправлены обновление дубликатов ключей/секций INI и загрузка неверного hotkey без потери остальных назначений. Остановка RVC ограничивает ожидание `taskkill` и самого дочернего процесса двумя секундами на этап; ошибка передаётся UI и сохраняется в `results/rvc-stop-error.log`. При отказе ОС завершить процесс он может остаться работающим, но этот путь больше не вызывает безусловный `wait()`. Release-сборка, 6 Rust-тестов и Clippy `--all-targets -- -D warnings` прошли. Тест завершения использует отдельный служебный процесс, без загрузки модели. Подробности: [повторное ревью](results/quality-review-20260917.md).
- RVC: `src/rvc_check.py` проверяет реальный локальный сервер синтетическим сигналом: все размеры блока, конечный PCM нужной длины, изменение тона и отказ для неверных параметров. В прогоне блок 200 мс: среднее время HTTP + inference после прогрева 81,7 мс; 100 мс: пик 140,2 мс, поэтому меньший блок может не успевать под игрой. Это не измерение полной задержки микрофон → наушники. `mic_check --rvc-check`: 249/249 преобразованных блоков после прогрева, включая принудительный статус Bypass; Discord и выключенный RVC — точный обход. Rust controller test проверяет выбор слота, границы параметров, размеры блока, сохранение и клавиатуру. Общие OutputEffects прошли проверку. Логи: `results/rvc-controls-check.log`, `results/rvc-worker-check.log`. Прослушивание реальной речи и визуальная проверка нового экрана пользователем ещё требуются; окно игры при работе не переключалось.

## 2026-09-16 — миграция на Rust UI и постоянный TAG-хост

### Проверки новой версии

- Реверс: `effects_check` проверяет неизменность живого голоса при записи, ровно 7 200 тихих отсчётов (150 мс при 48 кГц), каждый отсчёт обратного воспроизведения с огибающей, плавное окончание 60 мс до точной тишины и возврат живого голоса за 10 мс, длительность, лимит 10 с, отмену, stale heartbeat, конфликт со скоростью и отсутствие старого хвоста. C ABI и Rust проверяют пятое назначение и дубликаты. Результаты: `results/reverse-check.log`, `results/reverse-controller-check.log`; интерфейс: `results/reverse-ui.png`. Прослушивание речи пользователем не заменяется этими тестами.
- Фразы: на тоне 440 Гц проверены длительность и частота для ×0,5 / ×0,7 / ×1,5 / ×2 (220 / 308 / 660 / 880 Гц), подавление alias при ускорении, точный обход, неизменный микрофон при записи скорости, лимит 10 секунд, отмена, устаревший heartbeat, конфликт клавиш и отсутствие старого хвоста. `results/phrase-dsp-check.log`. Преобразование — 32-отсчётный полифазный sinc-ресемплер с фильтром перед ускорением, в DSP-потоке; буфер заранее выделен. Новых зависимостей нет.
- Реальные TAG / VB-CABLE: `bin\mic_check.exe --smoke-phrases INPUT_INDEX TAG 14 2 40` (или индекс CABLE Input вместо TAG) проверяет запись/проигрывание обеих скоростей и сброс через Mute, без сохранения записи. `results/phrase-tag-check.log`, `results/phrase-cable-check.log`. Назначения четырёх клавиш и навигация проверены тестом Rust; Clippy чистый. Рендер UI — `results/phrase-ui.png`. Физические игровые нажатия и субъективное качество фраз в Discord ещё требуют пользовательской проверки.
- Прослушивание: реальные TAG и VB-CABLE → выход Windows; два включения, Mute до точной тишины, выключение вместе с движком, TAG без другого принимающего клиента. Главный тракт без underruns/drops. `results/monitor-tag-check.log`, `results/monitor-cable-check.log`; C ABI и клавиатурная навигация проверены, Clippy без предупреждений; снимок UI `results/monitor-ui.png`. Команда повторения: `bin\mic_check.exe --monitor-check INPUT_INDEX TAG`. Этот тест воспроизводит голос, но не сохраняет запись.
- Постоянный TAG: один открытый WASAPI-клиент пережил два полных уничтожения/создания движка, endpoint остался `{0.0.1.00000000}.{eee96b77-5abb-4312-ab7c-9173f7d095ad}`; все три остановленных фазы дали peak = 0. Повторены 12 переподключений клиентов без аудиопропусков. `results/persistent-tag-check.log`, `results/persistent-tag-reconnect.log`.
- Закрытие окна, повторное открытие и полный выход Rust UI: клиент получил 768960 кадров тишины, сохранил соединение и тот же endpoint. Принудительное завершение отдельного процесса обработки: клиент продолжил получать кадры; последующий двухсекундный замер дал точную тишину. `results/persistent-tag-ui-receiver.log`, `results/persistent-tag-crash-*.log`. При аварии возможен короткий перерыв до watchdog; это не бесшовное продолжение речи.
- Фоновый TAG-хост: рабочий набор 5,64 МБ в коротком замере, NVIDIA/CUDA-библиотек в процессе нет; автозапуск текущего пользователя зарегистрирован. `results/persistent-tag-host.json`. Перезагрузка Windows ради проверки автозапуска не выполнялась.
- `verify.ps1`: C++ проверки очереди/дрейфа, DSP и C ABI; Rust-тесты INI, контроллера и завершения дочернего процесса; строгий Clippy. Контроллер использует настройки в памяти и не запускает tray/TAG shell, поэтому закрывать работающую программу перед Rust-тестами не требуется.
- DSP: точный dry bypass / нейтральные значения; конечный ограниченный сигнал; частотные отношения для −12, −5, +7, +12 полутонов; отпускание и отсутствие старого хвоста; watchdog, epoch, сочетания и одновременные эффекты.
- TAG: отсутствующий endpoint и восстановление, 3 запуска, 12 подключений, 48 кГц stereo / 44,1 кГц mono, Mute; без underruns/drops. `results/rust-lifecycle.log`.
- Реальные TAG и VB-CABLE с переключением обоих эффектов и Mute, по 12 секунд: без underruns/drops; TAG driver gaps = 0. Максимум обработки pitch в этих прогонах 1,82 / 1,08 мс на блок. `results/rust-effects-tag.log`, `results/rust-effects-cable.log`. Это синтетическое управление эффектами в движке, не физическое нажатие клавиш в игре. Голос не записывался.
- Ресурсы остановленного Rust UI в коротком замере: открытое окно 20,08 МБ / 1,17% одного ядра, трей 18,16 МБ / 0,00% (округление), повторно открытое 20,60 МБ / 0,39%. Старый скрытый Win32 UI: 12,12 МБ / 0,00%. Новый интерфейс требует больше памяти; снижение расхода ресурсов относительно старого UI не заявляется. Это не измерение под нагрузкой игры.
- Реальные снимки программного рендерера: главное окно, настройки, масштаб 200%, маленькое окно. `results/rust-ui-*.png`. Метрики открытого/скрытого окна — `results/rust-ui-resources.csv`.
- **Пока не проверено человеком:** субъективное звучание pitch/насыщения на вашей речи в Discord; реальные игровые хоткеи; физическое извлечение USB, блокировка/сон и перезапуск Explorer. Код обработки этих событий есть; не выдаём программные тесты за физическую проверку. FPS не измерялся. Настройки Discord автоматически не менялись; его AGC может уменьшать заметность boost.

### Измерение задержки

2026-09-16: повторный одновременный 14-секундный захват QuadCast S и CABLE Output с речью при v2, intensity 100%, reserve 40 ms дал **около 248 мс**. Корреляция формы сигнала в шести отрезках дала 247,88–249,00 мс; общая корреляция огибающей — 0,931 с максимумом при 248 мс. Пики входа/выхода: 0,1316 / 0,0890. Это измерение задержки, а не оценка качества шумоподавления. Нужен отдельный замер для разделения задержки NVIDIA, Windows и кабеля. Результат: `results/latency-20260916-2/analysis.json`. Первый захват (`latency-20260916-1`) дал около 245 мс, но пользователь не говорил; он не считается замером речи.

Пакеты выровнены по QPC timestamps первого отсчёта; часы каждого устройства аппроксимированы линейно, чтобы учесть дрейф и джиттер меток драйвера. Во втором замере ошибок timestamp и discontinuity в обоих захватах нет; максимальное отклонение меток от аппроксимации: 0,08 мс у микрофона, 1,46 мс у кабеля. Метод оценивает задержку между точками захвата Windows, без акустической/ADC задержки и без Discord. Точность до сотых миллисекунды не заявляется.

Дополнительная проверка существующих файлов официального `effects_demo`: корреляция исходного `Typing_48k.wav` с выходами v1 и v2 дала сдвиг **70 мс** (коэффициент около 0,998). Результаты: `results/offline-model-delay.json`. Это сдвиг сигнала в файловом тесте, не длительность GPU-вызова; в тех конфигурациях `enable_vad=0`, тогда как приложение использует значение SDK по умолчанию. Поэтому 70 мс нельзя без отдельной проверки считать точным вкладом модели в живые 248 мс. Наш резерв очереди составляет ещё 40 мс. У VB-CABLE есть собственная настраиваемая задержка (в руководстве по умолчанию 7168 отсчётов, около 149 мс при 48 кГц); фактическое активное значение кабеля пока не считано. На этом этапе задержка Broadcast ещё не была измерена; последующее сравнение приведено ниже. BNR 2.0 официально обозначен experimental и ориентирован в том числе на ASR; более новая версия сама по себе не доказывает преимущества над Broadcast в голосовом чате.

### Одновременное сравнение с Broadcast и отдельный тест кабеля

Замер 2026-09-16 (`results/routes-20260916-1`) использует одну живую речь, одновременно захваченную на четырёх endpoints. Пользователь подтвердил QuadCast S + только Noise Removal в Broadcast; MicNoiseReducer оставлен на v2 / 100% / 40 ms.

| Путь от QuadCast | Сдвиг по форме сигнала | Диапазон по отрезкам |
|---|---:|---:|
| NVIDIA Broadcast Microphone | 176,69 мс | 176,56–176,69 мс |
| MicNoiseReducer → CABLE Output | 254,81 мс | 254,44–255,50 мс |

Разница около **78 мс**. Это сравнение задержки при текущих настройках, без оценки качества шумоподавления или FPS.

**Loopback CABLE Input не является надёжной точкой «до кабеля».** В этом драйвере/режиме соответствующий сигнал получил временные метки примерно на 86 мс позже CABLE Output (согласованность сигналов высокая). Поэтому результаты loopback не использованы для выделения внутренней задержки кабеля; отрицательный сдвиг учтён в анализаторе. Отчёты: `loopback-endpoint.json`, `loopback-to-output.json`. Результат описывает наблюдаемые метки endpoint, а не доказанное место буфера внутри драйвера.

Вместо этого `--probe-cable` отправил непериодический тестовый сигнал непосредственно в CABLE Input и сопоставил каждый выбранный блок с CABLE Output. Медиана от QPC при отправке до метки захвата — **88,93 мс**, диапазон 84,50–95,24 мс, корреляция каждого из 11 блоков >0,998. Величина включает Windows и очередь отправки, это не чистая задержка драйвера. После приблизительного вычитания известного заполнения render-буфера медиана 86,10 мс. Отчёт: `results/cable-probe-20260916-1/probe-analysis.json`. Дополнительный путь через кабель — основное подтверждённое направление для сокращения разницы с Broadcast; точная аддитивная раскладка всего тракта пока не получена. Настройки приложений и драйвера в ходе теста не изменялись.

Повторный речевой замер через TAG `results/tag-latency-20260916-2/analysis.json`: 14 секунд, v2/40 мс, задержка по огибающей **120 мс**, по двухсекундным участкам **114–124 мс**, корреляция 0,987. Ошибок QPC и флагов discontinuity в записи нет; остаточная ошибка временной шкалы TAG достигает 4,11 мс. Поэтому это приблизительная оценка, не точность до одного отсчёта. Она не включает акустический/ADC тракт и Discord. Предыдущие отдельные записи дали 255 мс через VB-CABLE и 177 мс у Broadcast — улучшение относительно них, без одновременного A/B с Broadcast в новом замере. В полном прогоне накопились 6 underruns и 4 сброса; максимальный вызов NVIDIA — 224,31 мс, включая планирование CPU/GPU. Нулевые счётчики драйвера не означают отсутствие этих пропусков. Причина редких длительных вызовов пока не установлена; равенство качества и влияние на FPS не измерены.

### Проверки на текущем ПК

RTX 3090, Ryzen 7 7800X3D, драйвер 596.49, QuadCast S, Windows 11. NVIDIA DLL загружаются из каталога проекта — проверено по модулям процесса. Системный SDK и Broadcast не изменены.

- Самопроверка: заполнение/переполнение очереди, оборачивание индексов, порядок 200 000 отсчётов между потоками, компенсация ±1000 ppm дрейфа за 10 минут моделируемого времени.
- Denoiser v2: 60 с, изменение интенсивности и mute, 5992 блока, **0 underruns, 0 drops**. CABLE Output принял 2 908 320 отсчётов; максимальная амплитуда 0,0983. Запись на диск не велась. Лог: `results/native-v2.log`.
- Максимальный вызов обработки v2: 27,56 мс, включая планирование CPU/GPU. Оба устройства использовали период 10 мс. Один флаг discontinuity зарегистрирован при запуске захвата.
- Два минутных прогона v1 дали пропуски: при запасе 40 мс — 2 underruns / 4 drops, при 80 мс — 2 / 2, максимальный вызов до 152,65 мс. Последующий 15-секундный прогон v1 с запасом 40 мс прошёл без пропусков (максимум 31,08 мс). Сам вызов изменения интенсивности в последнем тесте занимал не более 0,0074 мс; причина редких длинных пауз SDK не установлена. Нехватки VRAM не обнаружено (свободно более 21 ГБ). v1 не считается подтверждённым стабильным режимом под текущей нагрузкой; логи `results/native-v1.log`, `results/native-v1-80ms.log` сохранены.
- Это короткие проверки при текущей нагрузке, а не доказательство отсутствия влияния на FPS или равенства качества Broadcast.

### Прототип TAG и диагностика NVIDIA

TAG — проприетарная demo; условия постоянного использования пока не подтверждены. Начальный прогон с 20 мс дал пропуски; он не считается стабильным режимом. Первый речевой замер прервался из-за завершения тестового хоста по таймеру и не даёт достоверной задержки.

Контрольный A/B CUDA Graphs по 60 с (6000 блоков, `results/afx-comparison.json`): штатный режим p95/p99/max = 3,00/14,21/37,89 мс; Graphs = 5,27/15,02/29,77 мс. В обоих 127 вызовов длиннее 10 мс, ни одного длиннее 40 мс; PCM совпал побитно на этом входе. Устойчивого выигрыша Graphs не установлено, поэтому штатный режим сохранён. CPU-время `GetThreadTimes` имеет грубую дискретность и не позволяет надёжно отделить GPU-ожидание от вытеснения CPU. Без ETW нельзя приписать причину одному планировщику. Первые `afx-default.csv`/`afx-graphs.csv` использовали `sleep_until` с джиттером до периода системного таймера и не используются как финальное сравнение. Контрольные суммы всех 5 файлов из `nvidia-assets.sha256` совпали.

Повторный живой прогон (`results/tag-diagnostic2.log`, `tag-diagnostic2-receiver.log`): хост 120 с, приём 90 с / 4 320 480 отсчётов, 0 underruns/drops/ошибок TAG. Максимум Run 3,81 мс, Reset 0,65 мс. Это успешный короткий прогон, но не установление причины прежних редких пауз. Broadcast автоматически перезапустился через `NvBroadcast.Container.exe` после завершения процесса; прогон не считается тестом без Broadcast. Службы, автозапуск и системные настройки NVIDIA не менялись.

Проверка интегрированного движка `mic_check --smoke 4 TAG 60 2 40` (`results/tag-integrated-smoke.log`) прошла: 6067 блоков, 0 underruns/drops/TAG gaps, максимум Run 3,20 мс, Reset 0,22 мс, изменение интенсивности 0,011 мс. Проверены приём виртуального микрофона, смена интенсивности, mute/unmute, остановка и отказ второму TAG-хосту. Один capture discontinuity был при первоначальном запуске. Исходники, GUI и CLI собраны Release; UI использует тот же движок, визуальная автоматическая проверка окна в этой среде недоступна. Тест FPS/frametime пользователь отложил. Причина прежних редких пауз остаётся открытой; успешные короткие прогоны её не опровергают.

### Проверки надёжности и диагностика

- Перезапуск из трея: Release-сборка и Rust-тесты прошли; отдельный бит события не пересекается с Exit/завершением сеанса. Фактический клик по системному меню трея не автоматизирован.
- Иконка приложения: Release-сборка и Rust-тесты прошли; из собранного EXE успешно извлечён встроенный 32×32 кадр многоразмерного ICO. Iced декодирует встроенный RGBA PNG, а tray загружает малый кадр ресурса ID 1 с системным fallback.
- Доработка UI и индикации: Release-сборка и `--self-test` прошли. `results/ui-polish-lifecycle.log`: 12 подключений TAG, 3 старта движка, 48 кГц stereo / 44,1 кГц mono, восстановление после отсутствующего входа; 0 пропусков, muted-выход и его индикатор строго нулевые, остановка очищает состояние выхода. `results/ui-polish-cable.log`: 15 с, 1468 блоков, 725 760 принятых отсчётов, 0 underruns/drops, максимум обработки 3,16 мс. Голос не записывался; субъективное качество этими проверками не измерялось. Нативная визуальная автоматизация недоступна: внешний вид и восстановление значка после реального перезапуска Проводника не проверены автоматически.
- В резервном маршруте WASAPI добавлена проверка отсутствия пакетов микрофона в течение 2 секунд: события выхода больше не скрывают остановку захвата. После сброса/опустошения очереди обнуляются регулятор дрейфа и выставленная частота выхода. Физическое отключение USB и принудительное опустошение очереди в этой проверке не воспроизводились.
- Выбор устройств, версии и запаса очереди сохраняется при изменении, без необходимости нажимать «Запустить». Отсутствующий сохранённый микрофон/выход больше не заменяется другим автоматически; требуется «Обновить» или явный выбор. Логика выбора покрыта `--self-test`.
- После underflow/сброса очереди TAG обнуляет накопленную коррекцию частоты: старое отклонение не переносится на заново заполненный буфер. Счётчик переданных кадров теперь 64-битный и не оборачивается примерно через сутки.
- `mic_check --lifecycle-tag 4`: успешно проверены ошибка отсутствующего входа без подмены микрофона, последующий нормальный запуск, 3 старта движка, 12 подключений клиента, чередование 48 кГц stereo / 44,1 кГц mono. Во всех muted-проходах выход строго нулевой; пропусков не зарегистрировано. Лог: `results/tag-lifecycle.log`. Это проверка жизненного цикла и отсутствующего endpoint, **не физическое извлечение USB-кабеля**. Появление endpoint в Windows асинхронно; тест ждёт его регистрации.
- Контролируемая подача речи/тишины 180 с: 18 000 блоков; p95 2,96 мс, p99 4,20 мс, максимум Run 8,33 мс; ни одного вызова свыше 10 мс. Результат: `results/afx-soak-180.json`. Проверяется SDK с записанной речью, не весь живой тракт. Прежние редкие паузы до 224 мс в этом прогоне не воспроизведены; их первопричина не доказана.
- После остановки/закрытия движка в `results/sessions.log` добавляется итог сеанса: UTC, режим, версия, запас, максимальные времена Run/Reset, пропуски и статус. Голос туда не записывается; файловые операции выполняются после завершения аудиопотоков. При аварийном завершении процесса итог может не сохраниться. Невозможность записать диагностический лог не препятствует остановке аудио.
- Финальная проверка пересобранного движка 15 с (`results/tag-final-smoke.log`): 1524 блока, максимум Run 8,29 мс, 0 underruns/drops/TAG gaps. Проверены mute/интенсивность и фактическая запись сводки в `sessions.log`.
- Для прослушивания: `results/listening-comparison/SDK_v2.wav` и `Broadcast.wav`. Это один прежний одновременный захват, примерно выровненный по задержке, с отдельным постоянным усилением для одинакового RMS. SDK-файл получен через прежний VB-CABLE, **не является новой записью TAG**. Метаданные в `comparison.json`; субъективное качество и подавление другого говорящего нельзя доказать одной корреляцией.

После исправления часов: минутный приём TAG с запасом 40 мс — 2 880 960 отсчётов, пик 0,53125, 0 ошибок драйвера и 0 опозданий хоста сверх 10 мс (максимальный интервал 2,96 мс). При этом зарегистрированы 1 underrun и 1 сброс очереди, максимальный вызов NVIDIA 103,72 мс. Это рабочий транспорт, но не подтверждённый стабильный режим: длительную паузу SDK текущий запас не покрывает. Логи: `results/tag-fixed-40ms.log`, `results/tag-receive-40ms.log`. Самопроверка `mic_check --self-test` дополнена сохранением часов при позднем пробуждении и проходит.

### Исследование драйвера

Проверка собственного виртуального микрофона и готового подписанного транспорта без VB-CABLE: [driver-feasibility.md](driver-feasibility.md). TAG demo установлен, отдельный C++-хост `mic_tag` собран и передаёт звук через API прямого буфера. Свой драйвер требует отдельного решения вопроса подписи.
